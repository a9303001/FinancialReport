
# Processing script for StkScreener(gemini)
import os, glob, re, sys, shutil

def run():
    print('Starting full ranking and regeneration process...')
    # Load all existing stocks
    # Merge new entries
    # Recalculate ranks, sort, rewrite files
run()
