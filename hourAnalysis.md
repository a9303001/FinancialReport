/goal

使用latest claude OPUS model

## 1. 任務目標（Goal）

**分析標的**：7203Toyota，jp
美股:TM

### 1.1 核心問題（要回答的重點）
1. 關稅對每股影響
2. 美國佔EPS比重
3. 電動車影響
4. 如何因應中國電動車和自駕車衝擊
2. 自駕車影響
3. 自駕車進度

### 1.3 輸出位置
- <公司folder>: `7203Toyota`

---

## 2. 資料來源（Data Sources · 依序）

1. **前一輪的 `hourAnalysisResult.md`** — 作為延續與比對的基準（每次先讀回）。
2. **FinancialReport repo 內的財報與輿情檔** 
3. **網路搜尋（web search）** — 取得最新股價、PE、財報、新聞與市場動態，並與本地資料交叉驗證。

---

## 3. 研究深度（Research Depth）

- 進行 **deep research**：盡量搜尋、交叉驗證，資訊要新、要可驗證。
- 資料缺失時，**明確註記缺口與可能原因**（如財報尚未發布、資料庫延遲等）。
- **優先補齊前一輪仍缺的項目**（例如：5 年 ROE 完整年序、最新一季實績），逐輪把缺口補上。
- 必須同步執行 AGENTS.md 中的RULE。

---

## 4. 輸出 hourAnalysisResult.md 規範（Output · 每次都要 optimize + rearrange）

> 本節為**與標的無關的泛用規範**：只透過 `<公司folder>` 佔位符與「第 1 節所定義的內容」等軟性指標引用任務，不寫死任何標的、幣別、題目或資料夾名稱。修改第 1 節時，本節無需一併更動。

### 4.1 核心原則
- **延續而非重寫（update, not append）**：每輪先讀回上一版，比對結論後「補新資訊、修正舊觀點」；不是把新內容堆在舊內容後面。
- **每輪都要 optimize + rearrange**：更新完內容後，整份檔案重新整理成「讓 user 一眼看得懂、簡單易懂」的狀態，**不可有流水帳**。
- **合併去重**：同一主題只留一處，觀點不得散落多段。
- **收斂舊內容**：過舊或已被推翻的內容直接移除或收進附註，只保留「仍有效」與「本輪有變化」的重點。
- **不保留版本歷史**：檔內只需一段最新的「更新時間戳 + 本輪變更摘要」，不需逐版 history。
- **可驗證**：關鍵數字一律附「公式／來源／假設」，讓 user 可自行核對。

### 4.2 固定結構（每輪都照此順序重排）
1. **更新資訊**：更新時間戳（`yyyy-MM-dd`）+ 本輪相較上一版的變更摘要（新增／修正／刪除各 1–3 條）。
2. **最新結論摘要**：直接回答「第 1 節所定義的核心問題」（不論題數），含「關鍵數據表格」（每股化）。
3. **計算與拆解**：本輪涉及的每項計算，逐項列公式、來源數據與假設，讓 user 可自行還原。
4. **多空對照**：以表格呈現利多／利空。
5. **資料缺口與待補**：仍缺的項目、可能原因，以及「下一輪想優先分析的項目」。

### 4.3 每股化（強制）
- 凡出現的財務絕對金額都要同步**每股化**（金額 ÷ (最新流通在外股數or基金單位數)），並在數字旁標註每股金額。
- **幣別**以分析標的的計價幣別為準（跨幣別換算需標明匯率與時點）。
- 換算需附「所用股數／單位數與其來源」，方便核對。

### 4.4 輸出位置與格式
- 更新至 `<公司folder>` 底下的 `hourAnalysisResult.md`（固定檔名，直接覆寫更新，不加日期後綴）。`<公司folder>` 依第 1 節設定。
- 以**表格／條列**為主，段落簡潔、標題清楚。

---
## 5. md 內容正確性檢查（Validation）

> 本節為**與標的無關的泛用規範**：僅以 `<公司folder>` 佔位符與「軟性指標」引用任務，不寫死任何公司、幣別、題目或資料夾名稱。**修改第 1 節時，本節無需一併更動**；檢查邏輯永遠只針對「當前檔案裡實際出現的內容」進行，而非針對特定標的。

### 5.1 檢查範圍（Scope）
- **基準檔（source of truth）**：以 `<公司folder>` 底下的 `hourAnalysisResult.md` 為準（其結論已於第 4 節 optimize、去重、收斂過）。
- **受檢檔**：`<公司folder>` 底下的其他 `.md` 檔。
- **排除**：**年報與季報** md 檔（屬原始財報來源，只作驗證依據，不得改寫）。

### 5.2 檢查項目（What to check）
逐一比對受檢檔，確認下列各點與基準檔一致且自洽：
1. **數字一致性**：同一指標（如 DPU、可配息金額、利息／本金、流通股數／基金單位數）在各檔之間數值一致，無互相矛盾。
2. **每股化正確**：金額 ÷ 股數／單位數的每股數字計算無誤，且所用股數／單位數與其來源與第 4.3 節一致。
3. **幣別與匯率**：幣別標註正確；跨幣別換算有標明匯率與時點。
4. **公式可還原**：關鍵數字附有「公式／來源／假設」，且依公式重算結果與檔內數字相符。
5. **日期與期別**：時間戳、財報期別（年度／季別）正確且非過期資料。
6. **內部邏輯與筆誤**：無前後文自相矛盾；無明顯單位錯置（億／萬、%、幣別符號）或錯字。

### 5.3 修正方式（How to fix）
- 發現錯誤時，**直接就地修改**受檢的 md 檔，**不另建新檔**、不加日期後綴。
- 僅修正「分析／衍生數據」的錯誤；**不得竄改原始財報數據**（原始數據若有疑義，於檔內以附註標明，而非逕改數字）。
- 若錯誤源自基準檔本身，先回頭修正 `hourAnalysisResult.md`，再連動更新其他受檢檔，使全資料夾一致。
---

## 6. 收尾（Finalize）

- 每次分析完就 **commit + push**，並 **merge 回 `master` 分支**（不必等整個 session 結束）。
