/goal
# 全市場選股篩選（Stock Screener · Gemini 版）

> **執行規格書。每次執行前完整讀完本檔再開始。**

---

## 0. 三十秒理解本任務

| 做什麼 | 說明 |
|:---|:---|
| **目標** | 維護 Top 100 股票榜單，每輪抽「本輪額度」檔新標的驗證、評分、寫分析檔 |
| **標的來源** | 只能從 `StkScreenerResult\*.csv` 抽（唯一來源，不可自己想） |
| **篩選** | 七道門檻（G1~G7），全過才評分 |
| **評分** | 五維度共 100 分，通過不變式檢核 |
| **產出** | 每檔一個 `<排名>-<代號>-<公司>.md` ＋ 一個主結果檔 |
| **沒過門檻** | 立刻從 csv 刪那一列 → 存回 → 驗證 → 重抽（= SOP-DEL） |

### 檔案路徑

| 項目 | 路徑 |
|:---|:---|
| 規格檔（本檔） | `Routines_StkScreener_gemini.md` |
| 主結果檔 | `StkScreenerResult\0.StkScreenerResult_gemini.md` |
| 個股詳細檔 | `StkScreenerResult\<排名>-<代號>-<公司>.md` |
| 抽樣母體 | `StkScreenerResult\*.csv`（該目錄下所有 csv） |

### 個股檔命名

排名補零到 **2 位**，第 100 名用 **3 位**。
- ✅ `01-0883-中國海洋石油.md`　`57-MET-MetLife.md`　`100-OXY-Occidental Petroleum.md`
- ❌ `TOP01-...`　`1-...`　`0883-中國海洋石油.md`

### 專有名詞

| 名詞 | 意思 |
|:---|:---|
| **SOP-DEL** | 不合格標的的處理：從 csv 刪列 → 存回 → 驗證 → 重抽（§2） |
| **G1~G7** | 七項硬性門檻，全過才可評分 |
| **三大門檻** | G2（PE）、G3（營益率）、G4（ROE），抽樣當下先驗以省時間 |
| **本輪額度** | 每輪總共處理幾檔（見 §1 參數表） |
| **每市場配額** | 每個市場分配幾檔正式樣本（見 §1 參數表） |
| **說明缺口** | 榜上有名次但個股 md 不存在／內容殘缺／市佔未具名的檔數 |
| **榜上重跑** | 抽到已在 Top 100 的標的 → 不跳過，用最新數據重驗重評重寫 |
| **正式樣本** | 已通過七道門檻、佔用本輪額度的標的 |

---

## 1. 執行參數

| 參數 | 值 | 說明 |
|:---|:---|:---|
| **語言** | 繁體中文 | 專有名詞首次附英文，如 `營業利益率（Operating Margin）` |
| **抽樣母體** | `StkScreenerResult\*.csv` | 唯一來源。禁止自行想標的、用網路排行榜或記憶名單 |
| **🔧 每輪總額度** | **20 檔** | 補說明 ＋ 新抽 ＋ 榜上重跑合計上限（4 市場 × 每市場配額）。要改只改這格與下格 |
| **🔧 每市場配額** | **5 檔正式樣本** | 被拒標的不占配額 |
| **市場順序** | 台股 → 美股 → 港股 → 日股 | 一個市場完整做完才換下一個 |
| **榜單容量** | Top 100 | 依總分高到低，不足就照實列，嚴禁湊數 |
| **計算精度** | 子項 2 位 → 維度 1 位 → 相加 | 子項小數第 2 位；維度四捨五入到第 1 位；總分 = 5 維度直接相加 |
| **時間格式** | `YYYY-MM-DD_hh:mm:ss` | 結果檔與摘要的時間戳記 |

---

## 2. SOP-DEL：不合格標的處理程序

**何時觸發**：七道門檻任一道未過（含查不到、無法驗證、口徑不明、來源衝突、剛好等於邊界值）。
**唯一例外**：「市場歸屬判不出來」不算不合格 → 該列保留在 csv，記入「五、待查」。

### 五個動作（就地做完才准抽下一檔）

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 確認此檔來自**哪一個 csv** | 抽樣當下就要記住 |
| 2 | 用**股票代號完全比對**定位該列 | 不可用公司名模糊比對、不可依順序刪、不可整批清空 |
| 3 | 只刪這一列 | 其他列一個字元都不准動 |
| 4 | 寫回它原本那個 csv 的原路徑 | 不可另存新檔、不可搬到別的 csv |
| 5 | 重讀 csv 確認：BOM 還在、表頭完好、總列數剛好少 1、被刪代號查無 | 記一筆 `<代號> 從 <csv> 移除 — 卡在 G<X>` 進「二、修正紀錄」 |

⏱ 時序：`判定不合格 → 刪列 → 存回 → 驗證 → 才准抽下一檔`。不准合併多檔的刪除。

### csv 格式保存

各 csv 格式不一致，必須**沿用該檔原本的樣子**：

| csv | 編碼 | 表頭第一欄 | 引號 | 備註 |
|:---|:---|:---|:---|:---|
| `TW.csv` | UTF-8 with BOM | `代號` | 無引號 | 末欄「主要業務」為長文字 |
| `US.csv` | UTF-8 with BOM | `代碼` | 無引號 | 代號為英文字母 |
| `hk.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 代號 5 位含前導 0，前導 0 不可被吃掉 |
| `jp.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 數值含千分位逗號如 `"1,861"`，靠引號保護 |

> 上表供參，每輪仍以實際讀檔結果為準。

🔴 **最安全做法**：以原始文字讀進所有行 → 移除代號相符的那一行 → 其餘行原樣寫回。**不要**經過「解析成表格再重新序列化」，那是 BOM 與引號被破壞的主因。

### 寫入失敗時

csv 唯讀或寫入失敗 → 門檻判定照舊有效（仍要重抽），把「待刪列」記入「五、待查」與「六、下一輪待辦」，下輪重試。

---

## 3. 硬性門檻

### 七道門檻（G1~G7，全過才可評分）

🔴 **任一道未過 → 立刻跑 SOP-DEL**。

| # | 門檻 | 判定標準 | 未達標 |
|:-:|:---|:---|:---|
| **G1** | **市佔前 3 名（須具名）** | 產業整體 **或** 核心主力產品市佔前 3 名，須引述第三方數據（Gartner/IDC/Statista/Euromonitor/產業公會/年報，不採信公關宣傳）。🔴 必須同時寫出：①產品名 ②市場範圍 ③名次 ④市佔率% ⑤第 1~3 名競爭對手及市佔率 ⑥來源機構與日期。缺任一項即未通過 | 剔除。若「產業判定得出但查無第三方市佔」→ 標 `⚠️ 市佔資料不足`，記入「五、待查」 |
| **G2** | **PE 估值區間** | `5.0 < TTM PE < 15.0`（嚴格不含邊界）。TTM PE = 最新收盤價 ÷ 近四季 EPS；EPS ≤ 0 視為不符 | PE ≤ 5.0、≥ 15.0 或無法驗證 → 剔除 |
| **G3** | **營業利益率** | 最近完整年度 **> 10.0%** 且 TTM **> 10.0%**（兩者皆須達標，**都要寫出實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G4** | **ROE** | 5 年中位數 **> 10.0%** 且 最新/TTM **> 10.0%**（兩者皆須達標，**都要寫出實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G5** | **財務安全** | 依產業套用專屬指標（見下表） | 未達標 → 剔除；查不到 → 標 `資料缺漏`、財務安全計 0 分，記入「五、待查」 |
| **G6** | **排除 ADR 普通股** | ADR **特別股不排除** | ADR 普通股 → 剔除 |
| **G7** | **排除 OTC** | 必須在正規交易所掛牌（NYSE/NASDAQ/TWSE/TPEx/HKEX/TSE 等） | OTC/Pink Sheets → 剔除 |

> G2/G3/G4 = 三大門檻，抽樣當下先驗以省時間。
> 🔴 G3/G4 禁止用 `>10%` 交差 — 總覽與個股 md 都必須寫到小數點。

### 財務安全指標（G5，依產業五選一）

| 產業別 | 指標 | 門檻 |
|:---|:---|:---|
| 一般企業 | 負債比率 = 總負債 ÷ 總資產 | **< 70.0%** |
| 銀行 | 資本適足率（CAR） | **≥ 10.5%** |
| 金控 | 集團資本適足率 | **≥ 100%** |
| 保險 | RBC（美）/SMR（日）/SCR（歐） | **≥ 200%** |
| REIT | LTV（貸款價值比） | **< 50.0%** |

> 一檔只套一種指標，且必須與評分 §6.5 用的公式一致。

### 資料時效

| 資料類型 | 可接受時效 | 逾期處理 |
|:---|:---|:---|
| 股價 / PE | ≤ 5 個交易日 | 逾期 → 重新取價 |
| 財報數據 | 最新已公布年報/季報；TTM 含最近四季 | 只有更舊 → 標 `管理指標偏舊`，記入「五、待查」 |
| 市佔資料 | ≤ 24 個月 | 24~36 月可用須註明日期；**> 36 月 → `資料缺漏`**，市場地位計 0 分 |

> 所有數據都要標**來源 + 日期**。REIT 另需附股價淨值比、租金報酬率、空租率。

---

## 4. 抽樣母體：csv 候選池

### 讀取範圍

每輪開場列出並讀完 `StkScreenerResult\` 底下**所有** `.csv`，全部併起來當本輪候選池。沒有其他抽樣來源。

### 市場歸屬判定（依序判斷，第一個命中就採用）

| 順序 | 依據 | 規則 |
|:-:|:---|:---|
| 1 | **檔名**（優先） | `TW`/`台` → 台股；`US`/`美` → 美股；`HK`/`hk`/`港` → 港股；`JP`/`jp`/`日` → 日股 |
| 2 | csv 市場欄位 | 有 `market`/`exchange`/`市場`/`交易所` 欄位就採用 |
| 3 | 代號格式 | 5 位含前導 0 → 港股；純英文 → 美股；4 位純數字 → 台/日無法區分，標 `市場不明` |

> 台股與日股同為 4 位數字（`2330` vs `9101`），代號格式分不出來，所以檔名優先。
> 判不出來 → 標 `市場不明`，本輪跳過不抽，記入「五、待查」，**不刪 csv**。

### 欄位要求

至少能取得「股票代號」。公司名、市場欄位有就用，沒有就查補。**不要因欄位名對不上就重寫 csv 結構**。

### csv 既有欄位做「抽樣優先序」

csv 已含 `市盈率(TTM)`/`PER`、`經營利潤率`、`股息率TTM`、`ROE(%)`、`負債總額佔比(%)` 等欄位。

**用法（只影響「先抽誰」，不影響「過不過」）**：
- 優先抽 csv 上 PE 落在 6~14、營益率 > 12%、ROE > 12% 的列
- csv 上明顯出界的列仍須用即時數據確認後才可判定不合格
- 🔴 門檻判定一律以**即時查證的最新數據**為準；csv 數值不得當作通過依據，也不得抄進個股 md

### 候選池耗盡

某市場 csv 標的全抽完或全被刪 → 停掉該市場，在「一、執行摘要」與「五、待查」寫 `<市場> csv 候選池耗盡（剩餘 X 檔）`，在「六、下一輪待辦」請使用者補充 csv。不得用外部標的遞補。

---

## 5. 標準執行流程

### 快速執行卡（順序不可跳）

```
1. 讀本規格檔
2. 讀主結果檔 → 從「六、下一輪待辦」取得本輪稽核區間與待辦
3. 讀所有 .csv → 建立候選池
4. 稽核本輪指定的 20 檔總覽區間（5 輪循環，不占額度）
5. 算「說明缺口」→ 分配本輪額度
6. 抽樣（台→美→港→日）→ 驗門檻 → 不合格跑 SOP-DEL → 已在榜上則重跑
7. 全過 → 即時評分（含不變式檢核）＋ 即時寫個股 md（嚴禁批量後補）
8. 每 5 檔存檔一次，每做完一市場整體存檔一次
9. 重排 Top 100 → 重新命名 → 刪落榜檔 → 跑自查清單
10. 存檔 ＋ 對話框只回精簡摘要
```

**本輪最重要的產出**：每檔 Top 100 個股的 `<排名>-<代號>-<公司>.md`，內含完整 ①~⑦。主結果檔只是索引與總覽。

---

### 階段一：開場與健康檢查（步驟 1~3）

**步驟 1 — 讀入狀態**

1. 讀本規格檔。
2. 讀主結果檔（不存在就用 §8 骨架新建；讀取失敗**絕不可用空骨架覆蓋**）。
3. 讀「一、執行摘要」的 Checkpoint 與「六、下一輪待辦」，確定本輪稽核區間。
4. 列出並讀取所有 `.csv`，依 §4 建立四個市場候選池，記下各市場剩餘檔數。
   - 找不到任何 csv → 本輪不抽新股，只做稽核與補說明。

**步驟 2 — 分批稽核（每輪 20 檔，不占額度）**

用 **5 輪循環**（本輪是第幾輪看「六、下一輪待辦」）：

| 輪次 | 稽核區間 |
|:-:|:---|
| 1 | TOP 1~20 |
| 2 | TOP 21~40 |
| 3 | TOP 41~60 |
| 4 | TOP 61~80 |
| 5 | TOP 81~100 → 下輪回到 1 |

稽核三件事：
1. 總覽該區間 20 檔的數值與個股 md **是否 100% 一致**
2. 全鏈算術重算：`⑥ 財務數據 → 評分公式 → ⑦ 子項分 → 維度分 → 總分`
3. **逐檔套不變式 I1~I7**

發現不符 → 以公式算出來的為準直接改檔，記入「二、修正紀錄」。

**步驟 3 — 處理待補說明（優先於抽新股）**

```
說明缺口 = 總覽筆數 − 內容完整（①②③ 齊全 且 ⑤ 市佔已具名）的個股 md 檔數
```

| 缺口 | 額度分配 |
|:---|:---|
| **> 額度的 1/2** | 全部補個股 md，不抽新股，補完跳步驟 9 |
| **1 ~ 額度的 1/2** | 先補滿缺口，剩餘才抽新股 |
| **= 0** | 全部抽新股（各市場各一份每市場配額） |

---

### 階段二：市場迴圈（步驟 4~8）

**市場順序固定：台股 → 美股 → 港股 → 日股。** 一個市場完整跑完才換下一個。

**步驟 4 — 抽樣主迴圈（每市場湊滿「每市場配額」檔正式樣本）**

```
FOR 市場 IN [台股, 美股, 港股, 日股]:
    正式樣本 = 0
    WHILE 正式樣本 < 每市場配額:

        A. 候選池已空 → 記「候選池耗盡」→ 換下個市場

        B. 從 csv 抽 1 檔（依優先序），記住來自哪個 csv
           · 避開本輪已抽過的
           · 已在 Top 100 → 走 D（不跳過）

        C. 立刻查最新收盤價、EPS、營益率、ROE（不沿用 csv 值）
           算 TTM PE = 最新收盤價 ÷ 近四季 EPS
           驗 G2/G3/G4：
             ├ 未過 → 跑 SOP-DEL → 回 A（不占額度）
             └ 通過 → 進步驟 5 驗 G1/G5/G6/G7
                        ├ 未過 → 跑 SOP-DEL → 回 A
                        └ 全過 → 進步驟 6 → 正式樣本 += 1

        D. 【榜上重跑】（已在 Top 100，必做不可跳過）：
           1) 讀既有個股 md 的 ①~⑦
           2) 用最新數據重驗七道門檻、重算五維度（含不變式）
           3) 重寫 ①~⑦，同步更新總覽該列
           4) 重驗未過 → 踢出 + 刪個股 md + 跑 SOP-DEL
           5) 占額度，統計上算「更新」→ 正式樣本 += 1

        E. 每累積 5 檔正式樣本 → 立刻存檔（避免中斷時整批遺失）
```

**一律拒絕並跑 SOP-DEL 的情況**：門檻任一項未達、EPS ≤ 0、數據缺漏/過期、口徑不明、來源衝突。
**不可硬湊**：候選池抽乾湊不滿配額時，不得用邊界值、過期值或外部標的遞補。

**重抽軌跡記錄**：`市場｜來源csv｜代號｜被哪道擋下｜資料日期｜拒絕原因｜已否從csv移除`。
結果檔只在「一、執行摘要」寫重抽次數與移除筆數，不污染 Top 100 表格。

**步驟 5 — 產業判定與剩餘門檻**

1. 判定產業：`一般企業`/`銀行`/`金控`/`保險`/`REIT`（決定 G5 指標）
2. 逐條查驗 G1、G5、G6、G7

| 結果 | 處理 |
|:---|:---|
| 七道全過 | 進步驟 6 |
| 任一道未過 | 跑 SOP-DEL → 回步驟 4 重抽 |
| 數據不足 | 視為未過，同上 |

**步驟 6 — 即時評分與個股撰寫（單檔閉環）**

七道全過後，**立刻做完這四件事再處理下一檔**：
1. 依 §6 公式算五維度分數與總分
2. 套不變式 I1~I7 自檢，過不了就重算
3. 依 §7 模板寫完個股 md ①~⑦（榜上重跑則改寫最新）
4. 更新主結果檔「二、Top 100 總覽表格」該列

正確：`A 評分 → 寫 A.md → B 評分 → 寫 B.md`
錯誤：`A 評分 → B 評分 → C 評分 → 最後才補寫 md`

**步驟 7 — 單市場存檔**

該市場配額跑完或候選池耗盡後存檔，體檢：
- [ ] 主結果檔 `## ` 大標題剛好 3 個（一、二、三）
- [ ] 子項分與總分算術無誤，通過不變式
- [ ] 總覽表格與個股 md 數據一致、排名不重複
- [ ] 本市場被拒標的**每一檔**都跑完 SOP-DEL

**步驟 8 — 市場推進**

額度未用完且還有市場 → 回步驟 4 換下一市場；額度用完 → 步驟 9。

---

### 階段三：收尾與自檢（步驟 9~10）

**步驟 9 — 排名整合、落榜清理、收工自查**

1. **去重**：同一公司多地上市時，只保留主要上市地原股。
2. **重排序與重命名**：依總分高到低排（同分依 §6.7 判定），更新名次，改個股 md 檔名前綴。
3. **落榜檔強制刪除** 🔴：目錄下凡不在 Top 100 的個股 `.md` 一律刪除。`0.StkScreenerResult_gemini.md` 與所有 `.csv` **不可刪**（csv 只能刪「列」不能刪「檔」）。
4. **完成度指標**（填入「一、執行摘要」）：
   - **A** = 總覽排名筆數
   - **B** = 目錄下 Top 100 個股 md 檔數
   - **C** = ①②③ 齊全的檔數
   - **D** = ⑤ 市佔具名合格的檔數
   - 合格：`B ≥ A − 10` 且 `C = B` 且 `D = B`。若 C < B 或 D < B → 當場補齊。
5. **三大門檻全樣本稽核**：逐檔複查正式樣本的 G2/G3/G4（含 `>10%` 這種沒實際數值的寫法），不合格的跑 SOP-DEL 重抽。
6. **csv 刪列收尾稽核** 🔴：
   - 比對「被擋下的標的」與「已從 csv 移除的代號」→ 必須完全一致，有落差當場補做 SOP-DEL
   - 重讀四個 csv，確認 BOM/表頭/格式與改動前一致
   - 把移除筆數與剩餘候選數寫進「一、執行摘要」
7. **市佔敘述稽核**：掃描禁用詞（見 §7.1），命中處當場改寫並記入「二、修正紀錄」。
8. **分批稽核標註**：在「三、待補與追蹤清單」後方或個股 md 中註明下輪稽核區間。
9. **收工自查清單**（逐條打勾）：

**結構與檔名**
- [ ] 主結果檔 `## ` 大標題剛好 3 個（一、二、三）
- [ ] 無 `TOP01-...` 舊檔名殘留
- [ ] 目錄下無非 Top 100 的個股檔
- [ ] 總覽表格的排名、總分、公司名與個股 md 完全一致
- [ ] 總覽表格的連結都是 `](<檔名.md>)` 格式且檔案存在

**抽樣與門檻**
- [ ] 所有正式樣本 PE 嚴格在 5.0~15.0 之間（不含邊界）
- [ ] 營益率、ROE、安全指標都寫了實際數值，無 `>10%` 搪塞
- [ ] 所有標的都來自 csv，無外部來源
- [ ] 門檻判定用即時查證數據，非 csv 舊值
- [ ] 被擋下的標的都已當場跑完 SOP-DEL
- [ ] 榜上重跑的都已用最新數據重驗重評重寫

**csv 完整性**
- [ ] 被刪代號重讀 csv 確實查無；無另存/合併/新建 csv
- [ ] 被改動 csv 的 BOM/表頭/引號風格/編碼/換行與改動前一致
- [ ] 港股前導 0（`00151`）與日股千分位（`"1,861"`）寫回後未被破壞
- [ ] 無「判定不合格但還留在 csv」的標的

**內容品質**
- [ ] 每檔 ⑦ 五維度相加 = 標示總分
- [ ] 全數通過不變式 I1~I7
- [ ] 禁用詞掃描 0 處殘留
- [ ] 「五、下一輪待辦」已填
- [ ] Chat 只回精簡摘要

10. **填「五、下一輪待辦」**（每輪必填，不得留空）。

**步驟 10 — 最終存檔與回報**

1. 寫入 `StkScreenerResult\0.StkScreenerResult_gemini.md`。
2. 再讀一次驗證：日期當天、區塊沒變少、`## ` 大標題 3 個、個股檔連結有效。
3. **對話框只輸出精簡摘要（§9 格式）**，嚴禁貼完整檔案內容或大表格。

---

## 6. 五維度評分系統（滿分 100）

### 通用規則

```
clamp(min, max, x) = max(min, min(max, x))

計算順序：子項分（小數 2 位）→ 各維度分（四捨五入到小數 1 位）→ 總分 = 5 維度直接相加
```

| 維度 | 配分 |
|:---|:-:|
| 一、市場地位 | 25 |
| 二、成長性 | 25 |
| 三、獲利品質 | 20 |
| 四、估值 | 20 |
| 五、財務安全 | 10 |
| **合計** | **100** |

### 6.1 一、市場地位（25 分）

```
子項1  名次基準分（三選一）：第 1 名 = 18.0 ｜第 2 名 = 14.0 ｜第 3 名 = 10.0
子項2  市佔率加成：           min(4.0, 市佔率% × 0.1)
子項3  護城河加分：           每具備一項 +1.0，上限 3.0 → 只能是 0/1/2/3 的整數
                              （高轉換成本 / 網路效應 / 規模經濟 / 特許獨佔）
維度分 = clamp(0, 25.0, 子項1 + 子項2 + 子項3)
```

🔴 計分前提：
- 名次與市佔率必須來自 ⑤-1 的已具名資料。只寫「龍頭」「領先」無具名數據 → 子項 1、2 計 0 分。
- 護城河每加 1 分須在 ⑤-3 附量化證據，不得給 0.5 分。

### 6.2 二、成長性（25 分）

```
子項1  5 年淨利 CAGR：  clamp(0, 15.0, 5年CAGR% × 0.75)
子項2  10 年淨利 CAGR： clamp(0, 10.0, 10年CAGR% × 0.50)
維度分 = 子項1 + 子項2
```

> 無 10 年數據但有 ≥ 7 年可替代（須標註）；< 7 年 → 子項 2 計 0 分。

### 6.3 三、獲利品質（20 分）

```
OPM% = (營業利益率 ÷ 稅前淨利率) × 100%

子項1  OPM 分數：       max(0, 8.0 − |OPM% − 100| × 0.1)
子項2  營業利益率分數： min(6.0, 營業利益率% × 0.20)
子項3  5 年 ROE 中位數：clamp(0, 6.0, 5年ROE中位數% × 0.30)
維度分 = 子項1 + 子項2 + 子項3
```

> OPM 愈接近 100% 分數愈高（代表營業外損益干擾小）。

### 6.4 四、估值（20 分）

```
子項1  PE 分數：          clamp(0, 12.0, 12.0 × (15.0 − PE) ÷ 10.0)
子項2  5 年殖利率中位數： min(8.0, 5年殖利率中位數% × 1.60)
維度分 = 子項1 + 子項2
```

> 因 G2 規定 PE 嚴格在 (5, 15) 開區間，子項 1 合法值必在 (0, 12.0)。算出 ≥ 12.0 代表 PE ≤ 5.0，該檔應被 G2 擋掉 → 跑 SOP-DEL。

### 6.5 五、財務安全（10 分，依產業五選一）

```
一般企業： clamp(0, 10.0, 10.0 × (70.0 − 負債比%) ÷ 70.0)
銀行業：   clamp(0, 10.0, (CAR% − 10.5) × 2.0)
金控業：   clamp(0, 10.0, (集團適足率% − 100) ÷ 5.0)
保險業：   clamp(0, 10.0, (RBC% − 200) ÷ 15.0)
REIT：     clamp(0, 10.0, 10.0 × (50.0 − LTV%) ÷ 50.0)
```

### 6.6 缺失值與特殊情況

| 情況 | 處理 |
|:---|:---|
| 子項查無公開數據 | 計 **0 分**，標 `資料缺漏`，嚴禁臆測 |
| 稅前淨利率 ≤ 0 | OPM 子項計 0 分，標 `OPM 不適用` |
| 引用財務絕對金額 | 標來源日期，**強制換算每股金額** |
| 財務安全標註 | 寫明公式類型與代入值，如 `財務安全 6.2（REIT／LTV 19.0%）` |

### 6.7 同分排序

總分相同時依序：① PE 較低 → ② 5 年淨利 CAGR 較高 → ③ 財務安全較高 → ④ 代號升冪。

### 6.8 不變式檢核（評分與稽核時逐條套）

任一條違反 = 算術錯誤，當場重算並記入「二、修正紀錄」。

| # | 不變式 | 抓什麼 |
|:-:|:---|:---|
| **I1** | 市場地位 = `{18/14/10}` + `min(4, 市佔%×0.1)` + k，**k ∈ {0,1,2,3} 整數** | 護城河只能加整數，出現 23.5 必錯 |
| **I2** | 各維度 ≤ 配分上限（25/25/20/20/10） | 子項加總溢位 |
| **I3** | 估值子項 1 在 (0, 12.0) 開區間 | ≥ 12 → PE ≤ 5 → 應被 G2 擋 |
| **I4** | 總分 = 五維度（各已四捨五入到 1 位）直接相加 | ⑦ 驗算列必須等於五項相加 |
| **I5** | 若 ⑤-1 標 `⚠️ 市佔資料不足` → 市場地位 ≤ 3.0 | 沒具名卻拿 20 幾分 |
| **I6** | 總覽 PE/營益率/安全指標值三欄必須是具體數值 | 抓 `>10%` 或填錯欄位 |
| **I7** | 總覽該列五維度與總分和個股 md ⑦ 逐字相同 | 兩處不同步 |

---

## 7. 個股詳細檔撰寫規格

檔案路徑：`StkScreenerResult\<排名>-<代號>-<公司>.md`
必須包含 **① 至 ⑦**（⑤ 含 ⑤-1、⑤-2、⑤-3 三張必填子表）。

````markdown
### 第 N 名：<代號> <公司名稱> — 總分 XX.X / 100（<市場>／<產業別>）
- **本區塊最後更新**：YYYY-MM-DD
- **一句話定位**：<點名「什麼產品/服務」在「什麼市場」排第幾，100~500 字>

**① 為什麼能進 Top 100**（必列 3 點，每點 = 維度 + 分數 + 具名產品市場）
1. <例：市場地位 24.0/25 — 中國海域原油產量第 1、市佔 57.0%>
2. <例：成長性 21.4/25，5 年淨利 CAGR 達 22.5%>
3. <例：獲利品質 19.2/20，營益率 38.5% 且 OPM 95.8%>

**② 優點（買它的理由）**（3~5 點，每點 = 事實 + 數據 + 來源日期）
| # | 優點 | 具體數字 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**③ 缺點與風險（不買它的理由）**（3~5 點，嚴禁「無明顯風險」）
| # | 缺點 / 風險 | 具體數字或事件 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**④ 什麼情況會被踢出榜單**（2~3 條量化觸發條件）
- <例：TTM PE 升破 15.0（目前 X.X，尚有 XX% 緩衝）>

**⑤ 硬性門檻查驗**（🔴 五項都要寫實際數值，禁止 `>10%`）：
PE X.XX ✅｜營業利益率 XX.X%（年度 XX.X% / TTM XX.X%）✅｜ROE XX.X%（5 年中位數 XX.X%）✅｜<安全指標> XX.X% ✅｜非 ADR 普通股・非 OTC（<交易所>）✅

**⑤-1 市佔定位（🔴 六欄缺一即 `資料缺漏`）**
| 主力產品/服務 | 市場範圍 | 排名 | 市佔率 | 統計口徑 | 來源/日期 |
|:---|:---|:-:|:-:|:---|:---|
| <具體產品名，不可寫「本業」> | <全球/區域/國家> | 第X名 | XX.X% | <營收/出貨量等> | <機構, YYYY-MM-DD> |

**⑤-2 競爭格局（🔴 必填）**
| 排名 | 公司 | 市佔率 | 與本公司差距 |
|:-:|:---|:-:|:---|
| 1 | ... | XX.X% | ... |

**⑤-3 為什麼能排在這個名次**（2~4 點 = 可驗證機制 + 量化證據，不可只寫「品牌強」）
- <例：成本優勢 — 桶油成本 28.5 美元，低於 Shell 34.2、PetroChina 41.0（各公司 2025 年報）>

**⑥ 財務數據**：最新 EPS <金額/每股>｜預估 EPS｜5 年 ROE 中位數 XX.X%｜5 年殖利率中位數 XX.X%｜OPM XX.X%｜5 年淨利 CAGR XX.X%｜10 年淨利 CAGR XX.X%
（各項附來源日期；REIT 另附股價淨值比、租金報酬率、空租率）

**⑦ 分數拆解**：市場地位 XX.X（名次 XX.XX + 市佔 XX.XX + 護城河 X）｜成長性 XX.X（5年 XX.XX + 10年 XX.XX）｜獲利品質 XX.X（OPM XX.XX + 營益率 XX.XX + ROE XX.XX）｜估值 XX.X（PE XX.XX + 殖利率 XX.XX）｜財務安全 XX.X（<產業別>／<指標值>）
- **總分驗算**：XX.X + XX.X + XX.X + XX.X + XX.X = **XX.X**
- **不變式自檢**：I1 ✅｜I2 ✅｜I3 ✅｜I4 ✅｜I5 ✅｜I6 ✅
````

### 7.1 市佔敘述具名規範

**核心要求**：提到公司地位必須回答六個問題 — **哪個產品**、**哪個市場**、**第幾名**、**市佔多少%**、**贏過誰**、**憑什麼**。只寫「龍頭」不回答 → 視為未完成。

**禁用詞**（單獨出現即不合格）：`龍頭`、`領先`、`領頭`、`卓越`、`居前列`、`名列前茅`、`優秀標的`、`行業代表`、`具備高度行業競爭優勢`、`市佔率居區域前列`、`市場獨佔或領頭地位`。
> 例外：上述詞若**緊接**具體「產品 + 市場 + 名次 + 市佔率 + 來源」可保留作修飾語。

**改寫範例**：
| ❌ 不合格 | ✅ 合格 |
|:---|:---|
| 港股該行業龍頭代表標的，市場地位卓越。 | 中國海域原油產量排名第 1、市佔 57.0%（2025 年產量口徑，公司年報 2026-03-26）；第 2 名中石化 21.0%、第 3 名中石油 18.0%。 |

**多角化公司**：就營收占比最高的前 2 項業務分別列出名次與市佔率。
**查無第三方市佔**：⑤-1 標 `⚠️ 市佔資料不足`，子項 1、2 計 0 分，記入「五、待查」。

---

## 8. 主結果檔標準骨架

`StkScreenerResult\0.StkScreenerResult_gemini.md` **必須且只能包含以下 5 個 `## ` 大標題**（一~五），不可增減。個股詳細分析一律寫入獨立的 `<排名>-<代號>-<公司>.md`，不內嵌於主結果檔。

````markdown
# 全市場選股篩選結果（Gemini）
> 更新日期：YYYY-MM-DD hh:mm:ss
> 篩選條件：市佔前3 ∪ 產品市佔前3、嚴格 5<PE<15（不含邊界）、營業利益率>10%、ROE>10%、財務安全（一般 負債比<70%／銀行 CAR≥10.5%／金控 集團適足率≥100%／保險 RBC≥200%／REIT LTV<50%）、排除普通股 ADR 與 OTC（ADR 特別股不排除）

## 一、執行摘要
- **抽樣母體（csv）**：讀入 X 個 csv｜台 X／美 X／港 X／日 X｜市場不明 X
- **本次抽樣**：台 X／美 X／港 X／日 X（合計 X / <本輪額度>；僅計正式樣本）｜新入榜 X、榜上重跑 X
- **csv 淘汰刪列**：本輪移除 X 檔（台 X／美 X／港 X／日 X）；剩餘 台 X／美 X／港 X／日 X
- **初篩重抽紀錄**：台 X／美 X／港 X／日 X 次
- **Checkpoint 完成度**：`✅台 ✅美 ⬜港 ⬜日`（第 X 輪）
- **本次額度使用**：補說明 Z｜初篩 X｜門檻通過 Y｜深度分析 Y（合計 ≤ 本輪額度）
- **結果**：新入榜 X｜重跑更新 X｜落榜 X｜修正 X 處
- **Top 100 門檻分數**：XX.X 分
- **榜單**：X / 100 檔
- **說明完成度**：A=XX　B=XX　C=XX　D=XX
- **市佔敘述改寫**：本輪改寫 X 處（禁用詞：`✅ 0 處` / `⚠️ X 處`）
- **總覽分批稽核**：完成 TOP XX~XX（第 N/5 輪）｜修正 X 處｜不變式違反 X 處

## 二、修正紀錄 (Self-Fix Log)（只保留最新 5 筆）
| # | 標的 | 錯誤內容 | 修正後 | 依據來源 | 修正日期 |
|:-:|:--|:--|:--|:--|:--|

## 三、Top 100 排名總覽
> 「市佔定位」格式：`<產品>｜<市場>｜第 X 名｜XX.X%`，嚴禁「龍頭」「居前列」。
> 🔴 最後三欄填具體數值：PE 填 X.XX；營益率填 XX.X%（禁 `>10%`）；安全指標值填 `<指標名> XX.X%`（如 `負債比 38.2%`）。

| 排名 | 代號 | 公司 | 市場 | 產業別 | 市佔定位（產品｜市場｜名次｜市佔率） | 市場地位 (25) | 成長性 (25) | 獲利品質 (20) | 估值 (20) | 財務安全 (10) | 總分 | PE | 營益率 | 安全指標值 |
|:-:|:--|:--|:-:|:-:|:--|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:--|
| 1 | 0883 | 中國海洋石油 | 港股 | 一般 | 海域原油與天然氣開採｜中國海域｜第 1 名｜57.0% | 24.0 | 23.5 | 17.8 | 11.4 | 5.4 | 82.1 | 8.00 | 52.5% | 負債比 32.2% |

## 四、⚠️ 待查 / 資料不足清單
| 代號 | 公司 | 市場 | 卡關項目 | 已試過的來源 | 推測原因 |
|:--|:--|:-:|:--|:--|:--|
| 代號 | 公司 | 市場 | 卡關項目 | 已試過的來源 | 推測原因 |
|:--|:--|:-:|:--|:--|:--|

## 五、下一輪待辦（🔴 每輪必填）
- **待補個股說明**（最優先）：（代號 + 名次）
- **下一輪總覽稽核區間**：（如 TOP 21~40）
- **待改寫的空泛市佔敘述**：（代號 + 名次 + 目前寫法）
- **待修正的不變式違反**：（代號 + 違反哪一條）
- **csv 候選池狀況**：剩餘 台X／美X／港X／日X
- **待刪 csv 列（上輪寫入失敗者）**：（csv 檔名 + 代號）
- **下一輪要做的工作**：…
- **未跑完的市場**：…
````

---

## 9. 對話框回覆格式

每輪執行完，Chat **只能**回傳以下精簡摘要，**嚴禁貼完整 Markdown 或大表格**：

```text
✅ StkScreener(gemini) — YYYY-MM-DD_hh:mm:ss
- 完成市場：台/美/港/日（Checkpoint ✅✅✅✅）
- csv 候選池：讀入 X 個 csv｜本輪移除 X 檔（台X/美X/港X/日X）｜剩餘 台X/美X/港X/日X
- csv 刪列稽核：被擋下 X 檔 = 已刪列 X 檔 ✅（BOM/格式驗證通過）
- 額度使用：補說明 X + 新分析 Y + 榜上重跑 W（合計 Z / <本輪額度>）
- 初篩稽核：正式樣本 Z / <本輪額度> 皆通過七道門檻 ✅｜重抽 X 次
- 結果：新入榜 X｜重跑更新 X｜落榜 X｜修正 X 處
- 不變式：I1~I7 違反 0 處 ✅（本輪修正 X 處）
- 榜單：X / 100（門檻分數 XX.X）
- 說明完成度：A=XX　B=XX　C=XX　D=XX
- 市佔具名：禁用詞殘留 0 處 ✅｜改寫 X 處
- 總覽稽核：完成 TOP XX~XX（第 N/5 輪）｜修正 X 處
- 下一輪待辦：…
```
