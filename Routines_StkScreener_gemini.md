/goal
# 全市場選股篩選（Stock Screener · Gemini 版）

> **執行規格書。每次執行前完整讀完本檔再開始。**

---

## 0. 三十秒理解本任務

| 做什麼 | 說明 |
|:---|:---|
| **目標** | 維護 Top 160 股票榜單，每輪抽「本輪額度」檔新標的驗證、評分、寫分析檔 |
| **標的來源** | 只能從 `StkScreenerResult\*.csv` 抽（唯一來源，不可自己想） |
| **篩選** | 七道門檻（G1~G7），全過才評分 |
| **評分** | 五維度共 100 分，通過不變式檢核 |
| **產出** | 每檔一個 `<排名>-<代號>-<公司>.md` ＋ 一個主結果檔 |
| **沒過門檻** | 立刻從 csv 刪那一列 → 存回 → 驗證 → 重抽（= SOP-DEL） |
| 🔴 **每輪開場** | 先掃「三、Top 160 排名總覽」，**找不到對應個股 md 檔的列＝該股已被移出 Top 160** → 直接刪掉總覽該列，並把該代號從對應 csv 刪除（= **SOP-ORPHAN**，§2.5） |
| 🔴 **每輪稽核** | 對指定區間的 **20 檔既有榜上股**做**完整重新分析**：重查最新數據 → 重驗七道門檻 → 重算五維度分數 → 抓出個股 md 的錯誤 → 補充新資料（= **SOP-AUDIT**，§5 步驟 2）。**不占本輪額度** |
| 🔴 **每輪收尾** | 榜單定案後，把 **Top 160 全部代號從各 csv 刪列**，讓 csv 只留「尚未入榜的候選」（= **SOP-SYNC**，§2.4）。**每一輪都要做，不可略過** |

### 檔案路徑

| 項目 | 路徑 |
|:---|:---|
| 規格檔（本檔） | `Routines_StkScreener_gemini.md` |
| 主結果檔 | `StkScreenerResult\0.StkScreenerResult_gemini.md` |
| 個股詳細檔 | `StkScreenerResult\<排名>-<代號>-<公司>.md` |
| 抽樣母體 | `StkScreenerResult\*.csv`（該目錄下所有 csv） |

### 個股檔命名

第 1~99 名排名補零到 **2 位**，第 100~160 名用 **3 位**（不補零到 3 位）。
- ✅ `01-0883-中國海洋石油.md`　`57-MET-MetLife.md`　`100-OXY-Occidental Petroleum.md`　`160-1264-德麥.md`
- ❌ `TOP01-...`　`1-...`　`001-...`　`0883-中國海洋石油.md`

### 專有名詞

| 名詞 | 意思 |
|:---|:---|
| **SOP-DEL** | 不合格標的的處理：從 csv 刪列 → 存回 → 驗證 → 重抽（§2） |
| **SOP-SYNC** | 每輪收尾的榜單同步刪列：把 Top 160 全部代號從 csv 刪掉（§2.4） |
| **SOP-ORPHAN** | 每輪開場的孤兒列清理：總覽有列但個股 md 不存在 → 刪總覽列 ＋ 刪 csv 對應列（§2.5） |
| **孤兒列** | 「三、Top 160 排名總覽」中，連結指向的個股 md **檔案不存在**的那一列。代表該股已被移出 Top 160 |
| **SOP-AUDIT** | 每輪稽核區間 20 檔的**重新分析程序**：重查 → 重驗 → 重算 → 抓錯 → 補新資料（§5 步驟 2） |
| **G1~G7** | 七項硬性門檻，全過才可評分 |
| **三大門檻** | G2（PE）、G3（營益率）、G4（ROE），抽樣當下先驗以省時間 |
| **本輪額度** | 每輪總共處理幾檔**新標的**（見 §1 參數表）。稽核 20 檔不計入 |
| **每市場配額** | 每個市場分配幾檔正式樣本（見 §1 參數表） |
| **說明缺口** | 榜上有名次但個股 md 不存在／內容殘缺／市佔未具名的檔數 |
| **正式樣本** | 已通過七道門檻、佔用本輪額度的標的 |
| **候選池** | csv 內**尚未入榜**的標的。入榜即刪列（SOP-SYNC），故 csv 與 Top 160 互斥 |

---

## 1. 執行參數

| 參數 | 值 | 說明 |
|:---|:---|:---|
| **語言** | 繁體中文 | 專有名詞首次附英文，如 `營業利益率（Operating Margin）` |
| **抽樣母體** | `StkScreenerResult\*.csv` | 唯一來源。禁止自行想標的、用網路排行榜或記憶名單 |
| **🔧 每輪總額度** | **20 檔** | 補說明 ＋ 新抽合計上限（4 市場 × 每市場配額）。要改只改這格與下格 |
| **🔧 每市場配額** | **5 檔正式樣本** | 被拒標的不占配額 |
| **🔧 每輪稽核批量** | **20 檔（8 輪循環走完 Top 160）** | 跑 SOP-AUDIT 完整重新分析，**不占本輪額度**，且**優先於抽新股執行** |
| **榜單／候選池互斥** | 每輪收尾跑 SOP-SYNC | Top 160 代號一律不得留在任何 csv；落榜標的**不回填** csv |
| **市場順序** | 台股 → 美股 → 港股 → 日股 | 一個市場完整做完才換下一個 |
| **榜單容量** | Top 160 | 依總分高到低，不足就照實列，嚴禁湊數 |
| **🔧 擴充期補位** | 榜單 < 160 檔時，每輪額度全數用於新抽樣 | 目前榜單自 Top 100 擴充至 Top 160，尚缺約 60 檔。每輪 20 檔 → 約 3 輪補滿。補滿前 §5 步驟 3「說明缺口 = 0」的情況一律走「全部抽新股」，**不得**因「榜單未滿」而放寬 G1~G7 |
| **計算精度** | 子項 2 位 → 維度 1 位 → 相加 | 子項小數第 2 位；維度四捨五入到第 1 位；總分 = 5 維度直接相加 |
| **時間格式** | `YYYY-MM-DD_hh:mm:ss` | 結果檔與摘要的時間戳記 |

---

## 2. csv 刪列程序（SOP-DEL 不合格淘汰／SOP-SYNC 入榜同步／SOP-ORPHAN 孤兒列清理）

> 三支程序的共同動作都是**從 csv 移除某些「列」**，差別只在觸發時機與對象。
> `SOP-DEL` = 抽樣當下、單檔、因不合格而刪；
> `SOP-SYNC` = 每輪**收尾**、批次、因已入榜而刪；
> `SOP-ORPHAN` = 每輪**開場**、批次、因總覽列找不到個股 md 而刪（**同時刪總覽列與 csv 列**）。
> 三者共用同一套 §2.2「csv 格式保存」規則與驗證方式。

### 2.1 SOP-DEL：不合格標的處理程序

**何時觸發**：七道門檻任一道未過（含查不到、無法驗證、口徑不明、來源衝突、剛好等於邊界值）。
**唯一例外**：「市場歸屬判不出來」不算不合格 → 該列保留在 csv，記入「四、待查」。

#### 五個動作（就地做完才准抽下一檔）

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 確認此檔來自**哪一個 csv** | 抽樣當下就要記住 |
| 2 | 用**股票代號完全比對**定位該列 | 不可用公司名模糊比對、不可依順序刪、不可整批清空 |
| 3 | 只刪這一列 | 其他列一個字元都不准動 |
| 4 | 寫回它原本那個 csv 的原路徑 | 不可另存新檔、不可搬到別的 csv |
| 5 | 重讀 csv 確認：BOM 還在、表頭完好、總列數剛好少 1、被刪代號查無 | 記一筆 `<代號> 從 <csv> 移除 — 卡在 G<X>` 進「二、修正紀錄」 |

⏱ 時序：`判定不合格 → 刪列 → 存回 → 驗證 → 才准抽下一檔`。不准合併多檔的刪除。

### 2.2 csv 格式保存（SOP-DEL／SOP-SYNC／SOP-ORPHAN 三者共用）

各 csv 格式不一致，必須**沿用該檔原本的樣子**：

| csv | 編碼 | 表頭第一欄 | 引號 | 備註 |
|:---|:---|:---|:---|:---|
| `TW.csv` | UTF-8 with BOM | `代號` | 無引號 | 末欄「主要業務」為長文字 |
| `US.csv` | UTF-8 with BOM | `代碼` | 無引號 | 代號為英文字母 |
| `hk.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 代號 5 位含前導 0，前導 0 不可被吃掉 |
| `jp.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 數值含千分位逗號如 `"1,861"`，靠引號保護 |

> 上表供參，每輪仍以實際讀檔結果為準。

🔴 **最安全做法**：以原始文字讀進所有行 → 移除代號相符的那一行 → 其餘行原樣寫回。**不要**經過「解析成表格再重新序列化」，那是 BOM 與引號被破壞的主因。

### 2.3 寫入失敗時

csv 唯讀或寫入失敗 → 門檻判定照舊有效（仍要重抽），把「待刪列」記入「四、待查」與「五、下一輪待辦」，下輪重試。
SOP-SYNC 寫入失敗同理：榜單照舊有效，未刪成功的代號記入「五、下一輪待辦」的「待刪 csv 列」，下輪開場第一件事就是補刪。
SOP-ORPHAN 寫入失敗同理：**總覽刪列照舊有效**（md 不存在的事實不會因 csv 寫不進去而改變），未刪成功的代號記入「五、下一輪待辦」的「待刪 csv 列」，下輪補刪。

---

### 2.4 SOP-SYNC：Top 160 入榜同步刪列（🔴 每輪收尾必做）

**核心規則**：**csv 是「尚未入榜的候選池」，Top 160 榜單上的標的一律不得留在任何 csv。**

**何時觸發**：§5 步驟 9 排名整合完成、Top 160 名單定案之後，**每一輪都要做，不可略過**。

🔴 **無條件執行**：這是本輪**最後一次動 csv** 的動作。即使本輪沒有新入榜標的、沒有稽核踢出、榜單一列都沒變，仍必須拿**定案後 Top 160 的全部 160 個代號**重跑一次刪列比對，**不得**以「上輪已經刪過了」為由略過。上輪漏刪、csv 被使用者補列、同代號重複列，都只能靠這一步抓出來。

#### 為什麼要刪

| 理由 | 說明 |
|:---|:---|
| 避免重複抽樣 | 榜上標的留在 csv 會被重複抽中，白白吃掉本輪額度 |
| 候選池收斂 | 每輪抽掉的、淘汰的、入榜的都離開 csv，剩餘數就是**真實可抽餘量** |
| 職責分離 | 榜上標的的維護改由 **SOP-AUDIT**（§5 步驟 2）負責，不再靠抽樣碰運氣 |

#### 執行步驟

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 取定案後的 **Top 160 全部代號**（不是只有本輪新入榜的） | 以「三、Top 160 排名總覽」為準 |
| 2 | 依「市場」把代號分成台／美／港／日四組 | 榜單的市場欄就是分組依據 |
| 3 | 每組**只比對該市場對應的 csv** | 🔴 台股 `1301` 與日股 `1301` 是不同公司，跨市場比對會誤刪 |
| 4 | 以原始文字讀進整份 csv，逐行比對第一欄代號，**完全比對**（去除引號與 BOM 後比） | 港股保留前導 0（`00836` ≠ `836`）；美股代號大小寫不敏感但要全等，不可用前綴比對（`CF` ≠ `CFG`） |
| 5 | 移除命中的行，其餘行**原樣寫回原路徑** | 不重新序列化、不另存新檔、不改欄位順序 |
| 6 | 重讀驗證：BOM 還在、表頭完好、`原列數 − 刪除數 = 新列數`、每個 Top 160 代號在對應 csv **查無** | 有落差當場修正 |
| 7 | 把「本輪同步刪列筆數／各市場剩餘候選數」寫進「一、執行摘要」 | 見 §8 骨架欄位 |

#### 邊界情況

| 情況 | 處理 |
|:---|:---|
| 代號在 csv 裡找不到 | 正常（前幾輪已刪過），**不算錯誤**，不必記錄 |
| 同一 csv 有多列同代號 | 全部刪除，並記入「二、修正紀錄」（csv 原始資料重複） |
| 本輪落榜／被稽核踢出的標的 | 🔴 **不回填 csv**。它們早已在入榜那輪被刪除，落榜後即退出候選池，本規格不提供復活機制 |
| 榜單市場欄為「市場不明」 | 不刪，記入「四、待查」，補完市場歸屬後下輪再刪 |
| csv 寫入失敗 | 依 §2.3 處理，記入「五、下一輪待辦」 |

🔴 **絕對禁止**：刪整個 csv 檔、清空 csv、把多個 csv 合併、為了「湊剩餘數」而新增列。**csv 只能刪列，不能刪檔，也不能加列。**

---

### 2.5 SOP-ORPHAN：榜單孤兒列清理（🔴 每輪開場必做）

**核心規則**：**「三、Top 160 排名總覽」的每一列都必須有對應的個股 md 檔存在。找不到檔 ＝ 這檔股票已被移出 Top 160 → 直接刪掉總覽該列，並把該代號從對應市場的 csv 刪除。**

**何時觸發**：§5 步驟 1（讀完主結果檔與所有 csv 之後、跑 SOP-AUDIT 之前），**每一輪開場都要做，不可略過**。

#### 為什麼要刪

| 理由 | 說明 |
|:---|:---|
| md 不存在＝已退出榜單 | 個股 md 只會因「稽核踢出」（§5 步驟 2 分流）或「落榜清理」（§5 步驟 9 第 3 點）而被刪除。檔沒了卻還留著總覽列，代表上輪只刪了檔、忘了刪列 |
| 修好完成度指標 | 孤兒列會讓 `A`（總覽筆數）> `B`（md 檔數），且總覽連結指向不存在的檔（等同 E8） |
| 不讓退榜標的復活 | 該代號一併從 csv 刪掉，確保它不會在後續輪次被重新抽中（與 §2.4「落榜標的不回填 csv」一致） |

#### 執行步驟

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 逐列取出總覽的 **排名／代號／公司／市場／連結檔名** | 一列都不可略過，含第 160 名 |
| 2 | 檢查連結檔名在 `StkScreenerResult\` 底下**是否真的存在** | 🔴 以**檔案系統實際存在**為準，不可只看連結文字長得對不對 |
| 3 | 檔案存在 → 保留該列，不做任何事 | 正常情況 160 列全部命中 |
| 4 | 檔案不存在（＝孤兒列）→ **直接刪除總覽該列** | 🔴 **不重新分析、不補寫 md、不試圖救回** |
| 5 | 依該列「市場」欄找到**對應市場的 csv**，用代號**完全比對**刪列 | 比對與寫回規則完全比照 §2.2／§2.4 步驟 4~5：只動該市場 csv、港股保前導 0（`00836` ≠ `836`）、美股全等不可前綴（`CF` ≠ `CFG`）、原始文字讀寫不重新序列化 |
| 6 | 每刪一檔記一筆「二、修正紀錄」 | 格式：`<代號> 個股 md 不存在（已移出 Top 160）→ 自總覽刪列，並自 <csv> 移除` |
| 7 | 清理後**不必當場重排名次** | 名次欄暫時不連號可接受，最終由 §5 步驟 9 第 2 點統一重排與重命名；但本輪 SOP-AUDIT 的稽核區間，以**清理後由上而下的順位**計算 |
| 8 | 把「孤兒列清理 X 列／同步 csv 刪列 X 檔／清理後總覽 X 列」寫進「一、執行摘要」 | 見 §8 骨架欄位 |

#### 邊界情況

| 情況 | 處理 |
|:---|:---|
| 檔案在，只是**排名前綴對不上**（md 為 `12-2496-卓越.md`，連結寫 `11-2496-卓越.md`） | 🔴 **不是孤兒列**。以代號 + 公司名找得到檔就算存在 → 屬 **E8 檔名／連結錯**，改連結或依 §0 命名規則更名即可，**嚴禁刪列** |
| 檔案在，只是公司名寫法有差（全形括號、空白、繁簡） | 同上，視為 E8 修正，不刪列 |
| 檔名含**空白或括號**（如 `53-00316-東方海外國際 (OOIL).md`） | 🔴 **最常見的誤判來源**：用正則／字串切割去抓 `[公司](檔名)` 時，檔名裡的 `)` 會讓連結被提早截斷，好檔被誤判成孤兒。務必拿**完整檔名字串**去做檔案存在檢查，確認真的不存在才判孤兒列 |
| 總覽列**連公司欄連結都沒有**（純文字公司名） | 用「代號」去目錄找 `*-<代號>-*.md`：找得到 → 補上連結（E8）；找不到 → 認定孤兒列，照第 4~6 步刪 |
| 代號在對應 csv 裡找不到 | 正常（入榜那輪已刪過），**不算錯誤**，不必記錄 |
| 該列「市場」欄為「市場不明」 | 總覽列照刪；csv 刪列改記入「五、下一輪待辦」的「待刪 csv 列」，補完市場歸屬後下輪再刪 |
| csv 寫入失敗 | 依 §2.3 處理：總覽刪列照樣有效，未刪成功的代號記入「五、下一輪待辦」 |
| 清理後榜單不足 160 檔 | **正常**。空缺由本輪或後續輪次的新抽樣自然補上，🔴 **嚴禁**為了湊滿 160 檔而放寬門檻或保留孤兒列 |

🔴 **絕對禁止**：為了讓總覽維持 160 列而保留沒有 md 的列；或為孤兒列臨時補寫一個 md 讓它「復活」。**md 不存在就是已退出榜單，本規格不提供復活機制。**

---

## 3. 硬性門檻

### 七道門檻（G1~G7，全過才可評分）

🔴 **任一道未過 → 立刻跑 SOP-DEL**。

| # | 門檻 | 判定標準 | 未達標 |
|:-:|:---|:---|:---|
| **G1** | **市佔前 3 名（須具名）** | 產業整體 **或** 核心主力產品市佔前 3 名，須引述第三方數據（Gartner/IDC/Statista/Euromonitor/產業公會/年報，不採信公關宣傳）。🔴 必須同時寫出：①產品名 ②市場範圍 ③名次 ④市佔率% ⑤第 1~3 名競爭對手及市佔率 ⑥來源機構與日期。缺任一項即未通過 | 剔除。若「產業判定得出但查無第三方市佔」→ 標 `⚠️ 市佔資料不足`，記入「四、待查」 |
| **G2** | **PE 估值區間** | `5.0 < TTM PE < 15.0`（嚴格不含邊界）。TTM PE = 最新收盤價 ÷ 近四季 EPS；EPS ≤ 0 視為不符 | PE ≤ 5.0、≥ 15.0 或無法驗證 → 剔除 |
| **G3** | **營業利益率** | 最近完整年度 **> 10.0%** 且 TTM **> 10.0%**（兩者皆須達標，**都要寫出實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G4** | **ROE** | 5 年中位數 **> 10.0%** 且 最新/TTM **> 10.0%**（兩者皆須達標，**都要寫出實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G5** | **財務安全** | 依產業套用專屬指標（見下表） | 未達標 → 剔除；查不到 → 標 `資料缺漏`、財務安全計 0 分，記入「四、待查」 |
| **G6** | **排除 ADR 普通股** | ADR **特別股不排除** | ADR 普通股 → 剔除 |
| **G7** | **排除 OTC** | 必須在正規交易所掛牌（NYSE/NASDAQ/TWSE/TPEx/HKEX/TSE 等） | OTC/Pink Sheets → 剔除 |

> G2/G3/G4 = 三大門檻，抽樣當下先驗以省時間。
> 🔴 G3/G4 禁止用 `>10%` 交差 — 總覽與個股 md 都必須寫到小數點。

### 財務安全指標（G5，依產業五選一）

| 產業別 | 指標 | 門檻 |
|:---|:---|:---|
| 一般企業 | 負債比率 = 總負債 ÷ 總資產 | **< 70.0%** |
| 銀行 | 資本適足率（CAR） | **≥ 10.5%** |
| 金控 | 集團資本適足率 | **≥ 100%** |
| 保險 | RBC（美）/SMR（日）/SCR（歐） | **≥ 200%** |
| REIT | LTV（貸款價值比） | **< 50.0%** |

> 一檔只套一種指標，且必須與評分 §6.5 用的公式一致。

### 資料時效

| 資料類型 | 可接受時效 | 逾期處理 |
|:---|:---|:---|
| 股價 / PE | ≤ 5 個交易日 | 逾期 → 重新取價 |
| 財報數據 | 最新已公布年報/季報；TTM 含最近四季 | 只有更舊 → 標 `管理指標偏舊`，記入「四、待查」 |
| 市佔資料 | ≤ 24 個月 | 24~36 月可用須註明日期；**> 36 月 → `資料缺漏`**，市場地位計 0 分 |

> 所有數據都要標**來源 + 日期**。REIT 另需附股價淨值比、租金報酬率、空租率。

---

## 4. 抽樣母體：csv 候選池

### 讀取範圍

每輪開場列出並讀完 `StkScreenerResult\` 底下**所有** `.csv`，全部併起來當本輪候選池。沒有其他抽樣來源。

🔴 **候選池語意（因 SOP-SYNC 而成立）**：csv 內留下來的都是**尚未入榜**的標的。
- csv 與 Top 160 **互斥**：正常情況下抽不到榜上標的。
- 若真的抽到已在 Top 160 的代號 → 代表上輪 SOP-SYNC 有遺漏 → **直接刪列（補做 SOP-SYNC）、不占額度、回頭重抽**，並記入「二、修正紀錄」。該檔的維護由 SOP-AUDIT 負責，不在抽樣階段處理。
- 曾入榜後又落榜的標的**不會回到 csv**，本輪不再抽到，屬預期行為。

### 市場歸屬判定（依序判斷，第一個命中就採用）

| 順序 | 依據 | 規則 |
|:-:|:---|:---|
| 1 | **檔名**（優先） | `TW`/`台` → 台股；`US`/`美` → 美股；`HK`/`hk`/`港` → 港股；`JP`/`jp`/`日` → 日股 |
| 2 | csv 市場欄位 | 有 `market`/`exchange`/`市場`/`交易所` 欄位就採用 |
| 3 | 代號格式 | 5 位含前導 0 → 港股；純英文 → 美股；4 位純數字 → 台/日無法區分，標 `市場不明` |

> 台股與日股同為 4 位數字（`2330` vs `9101`），代號格式分不出來，所以檔名優先。
> 判不出來 → 標 `市場不明`，本輪跳過不抽，記入「四、待查」，**不刪 csv**。

### 欄位要求

至少能取得「股票代號」。公司名、市場欄位有就用，沒有就查補。**不要因欄位名對不上就重寫 csv 結構**。

### csv 既有欄位做「抽樣優先序」

csv 已含 `市盈率(TTM)`/`PER`、`經營利潤率`、`股息率TTM`、`ROE(%)`、`負債總額佔比(%)` 等欄位。

**用法（只影響「先抽誰」，不影響「過不過」）**：
- 優先抽 csv 上 PE 落在 6~14、營益率 > 12%、ROE > 12% 的列
- csv 上明顯出界的列仍須用即時數據確認後才可判定不合格
- 🔴 門檻判定一律以**即時查證的最新數據**為準；csv 數值不得當作通過依據，也不得抄進個股 md

### 候選池耗盡

某市場 csv 標的全抽完或全被刪 → 停掉該市場，在「一、執行摘要」與「四、待查」寫 `<市場> csv 候選池耗盡（剩餘 X 檔）`，在「五、下一輪待辦」請使用者補充 csv。不得用外部標的遞補。

---

## 5. 標準執行流程

### 快速執行卡（順序不可跳）

```
1. 讀本規格檔
2. 讀主結果檔 → 從「五、下一輪待辦」取得本輪稽核區間與待辦
3. 讀所有 .csv → 建立候選池
4. 🔴 SOP-ORPHAN：掃「三、Top 160 排名總覽」每一列 → 個股 md **不存在**的列
   （＝已被移出 Top 160）→ 刪總覽該列 ＋ 從對應 csv 刪該代號
5. 🔴 SOP-AUDIT：對本輪指定的 20 檔榜上股「重新分析」
   （重查最新數據 → 重驗 G1~G7 → 重算五維度 → 抓 md 的錯 → 補新資料）
6. 算「說明缺口」→ 分配本輪額度
7. 抽樣（台→美→港→日）→ 驗門檻 → 不合格跑 SOP-DEL
8. 全過 → 即時評分（含不變式檢核）＋ 即時寫個股 md（嚴禁批量後補）
9. 每 5 檔存檔一次，每做完一市場整體存檔一次
10. 重排 Top 160 → 重新命名 → 刪落榜檔 → 🔴 跑 SOP-SYNC（Top 160 全數從 csv 刪列）→ 跑自查清單
11. 存檔 ＋ 對話框只回精簡摘要

> ⚠️ 本卡的編號是**執行順序**，與下方「步驟 1~10」的章節編號不對應（例如卡片第 5 項 = 步驟 2 SOP-AUDIT、卡片第 4 項 = 步驟 1 第 6 小點 SOP-ORPHAN）。交叉引用一律以「步驟 N」為準。
```

**本輪最重要的產出**：每檔 Top 160 個股的 `<排名>-<代號>-<公司>.md`，內含完整 ①~⑦。主結果檔只是索引與總覽。

---

### 階段一：開場與健康檢查（步驟 1~3）

**步驟 1 — 讀入狀態**

1. 讀本規格檔。
2. 讀主結果檔（不存在就用 §8 骨架新建；讀取失敗**絕不可用空骨架覆蓋**）。
3. 讀「一、執行摘要」的 Checkpoint 與「五、下一輪待辦」，確定本輪稽核區間。
4. 列出並讀取所有 `.csv`，依 §4 建立四個市場候選池，記下各市場剩餘檔數。
   - 找不到任何 csv → 本輪不抽新股，只做稽核與補說明。
5. **補做上輪未完成的刪列** 🔴：若「五、下一輪待辦」的「待刪 csv 列」非空 → 立刻依 §2.1／§2.4 補刪並驗證，再往下走。
6. **🔴 SOP-ORPHAN：榜單孤兒列清理（依 §2.5 八步驟，本輪第一件動總覽的事）**
   - 逐列檢查「三、Top 160 排名總覽」，連結指向的個股 md **檔案不存在**的列 ＝ 該股已被移出 Top 160
   - 該列**直接從總覽刪除**（不重新分析、不補寫 md），並把該代號從**對應市場的 csv** 刪除
   - 每刪一檔記入「二、修正紀錄」；刪完把筆數寫進「一、執行摘要」
   - ⚠️ 只是排名前綴／公司名寫法對不上但檔案找得到 → 屬 **E8**，改連結或更名即可，**不可刪列**
   - 必須做完這一步才能決定本輪 SOP-AUDIT 的稽核區間（區間以清理後的順位計算）
7. **開場一致性檢查**：抽查現行 Top 160 代號是否誤留在 csv（上輪 SOP-SYNC 是否漏做）→ 有殘留當場補做 SOP-SYNC，記入「二、修正紀錄」。

**步驟 2 — SOP-AUDIT：分批重新分析（每輪 20 檔，不占額度）** 🔴

用 **8 輪循環**（本輪是第幾輪看「五、下一輪待辦」）：

| 輪次 | 稽核區間 |
|:-:|:---|
| 1 | TOP 1~20 |
| 2 | TOP 21~40 |
| 3 | TOP 41~60 |
| 4 | TOP 61~80 |
| 5 | TOP 81~100 |
| 6 | TOP 101~120 |
| 7 | TOP 121~140 |
| 8 | TOP 141~160 → 下輪回到 1 |

> 🔴 榜單未滿 160 檔時，稽核區間**照名次區間取**，該區間內有幾檔就稽核幾檔（例如榜單目前 112 檔、本輪為第 6 輪 → 稽核 TOP 101~112 共 12 檔），**不得**挪用其他區間的標的來湊滿 20 檔，也不得因此跳過該輪。

🔴 **本步驟不是「對帳」，是「重做一次分析」**。榜上標的已從 csv 移除（§2.4），
往後**只有這裡**會再碰它們 —— 稽核做多細，榜單就多新。
禁止只比對數字就宣告通過；**沒有重新查證最新數據 = 本步驟未執行**。

#### A. 單檔七動作（一檔做完才做下一檔）

| # | 動作 | 要求 |
|:-:|:---|:---|
| **A1** | **讀回**既有個股 md 的 ①~⑦ 與總覽該列 | 記下目前總分，供事後比對分數變動 |
| **A2** | **重查最新數據**（不得沿用 md 舊值） | 最新收盤價、近四季 EPS、營益率（年度＋TTM）、ROE（最新＋5 年中位數）、安全指標、殖利率、淨利 CAGR、市佔率。每項都要有**來源 + 日期** |
| **A3** | **重驗七道門檻 G1~G7** | 標準與 §3 完全相同，不因「已在榜上」而放寬。時效不合（§3 資料時效）→ 重新取數 |
| **A4** | **重算五維度與總分** | 依 §6 公式從頭算，**不是**沿用舊分數微調 |
| **A5** | **套不變式 I1~I7** | 任一條違反 → 當場重算並記入「二、修正紀錄」 |
| **A6** | **抓錯**：逐條跑下方 **D. 稽核錯誤清單 E1~E9** | 命中即當場改檔 |
| **A7** | **補新資料**：把 A2 查到的新事實寫回 ②③④⑤⑥⑦，更新「**本區塊最後更新**」與「**本輪稽核**」兩行，同步總覽該列 | 新舊數據不同時以**新的為準**，舊值不必保留 |

#### B. 重驗結果分流

| 結果 | 處理 |
|:---|:---|
| **七道全過、分數變動 < 3.0 分** | 更新 md 與總覽，計「稽核維持」 |
| **七道全過、分數變動 ≥ 3.0 分** | 同上，另在「二、修正紀錄」寫明 `<代號> 總分 XX.X → XX.X（主因：<哪個維度><為什麼>）`，計「稽核重評」 |
| **任一道門檻未過** | 🔴 **踢出榜單**：刪該檔個股 md、從總覽移除、其餘名次遞補重排，在「二、修正紀錄」寫明卡在哪一道與實際數值，計「稽核踢出」 |
| **關鍵數據查不到／無法驗證** | 視同未過 → 踢出，並記入「四、待查」寫明查過哪些來源 |

> 被踢出的標的**不回填 csv**（§2.4 邊界情況），下輪不會再抽到。
> 踢出造成的空缺由當輪或後續輪次的新抽樣自然補上，**嚴禁**為了湊滿 160 檔而放寬門檻。

#### C. 存檔節奏

每稽核完 **5 檔**存檔一次；20 檔全部做完再整體存檔一次，然後才進步驟 3。

#### D. 稽核錯誤清單（E1~E9，A6 逐條套）

| # | 錯誤類型 | 怎麼抓 | 修法 |
|:-:|:---|:---|:---|
| **E1** | **算術錯** | ⑦ 子項相加 ≠ 維度分；五維度相加 ≠ 標示總分 | 以 §6 公式重算為準 |
| **E2** | **兩處不同步** | 總覽該列的五維度／總分／PE／營益率／安全指標值 ≠ 個股 md ⑦⑤ | 以重算後的新值同時覆蓋兩處 |
| **E3** | **不變式違反** | I1~I7 任一條（護城河非整數、維度溢位、估值子項 ≥12 等） | 重算並記錄 |
| **E4** | **模板缺漏** | ①~⑦ 缺區塊；⑤-1／⑤-2／⑤-3 缺表；① 不足 3 點；②③ 不足 3 點；④ 不足 2 條 | 補齊，缺資料就查 |
| **E5** | **數值搪塞** | 出現 `>10%`、`約`、`逾`、空白、`N/A` 卻仍給分 | 補實際數值；查無 → 該子項 0 分並標 `資料缺漏` |
| **E6** | **資料過期** | 股價/PE > 5 個交易日；市佔 > 24 個月；財報非最新已公布 | 重新取數；市佔 > 36 個月 → 市場地位計 0 分 |
| **E7** | **禁用詞殘留** | §7.1 禁用詞單獨出現、未接具名數據 | 當場改寫成「產品＋市場＋名次＋市佔率＋來源」 |
| **E8** | **檔名／連結錯** | 排名前綴補零錯、檔名與總覽連結不符、連結指向不存在的檔 | 依 §0 命名規則更名並修連結 |
| **E9** | **每股化缺漏** | 提到營收／營業利益／自由現金流等絕對金額卻沒換算每股金額 | 補上 `（換算約每股 X.XX <幣別>）` |

> 每命中一項就記一筆「二、修正紀錄」，並在「一、執行摘要」累計「稽核修正 X 處」。
> E1~E3 屬**算術與一致性**，E4~E9 屬**內容品質**，兩類都要跑，不可只跑一類。

**步驟 3 — 處理待補說明（優先於抽新股）**

```
說明缺口 = 總覽筆數 − 內容完整（①②③ 齊全 且 ⑤ 市佔已具名）的個股 md 檔數
```

| 缺口 | 額度分配 |
|:---|:---|
| **> 額度的 1/2** | 全部補個股 md，不抽新股，補完跳步驟 9 |
| **1 ~ 額度的 1/2** | 先補滿缺口，剩餘才抽新股 |
| **= 0** | 全部抽新股（各市場各一份每市場配額） |

---

### 階段二：市場迴圈（步驟 4~8）

**市場順序固定：台股 → 美股 → 港股 → 日股。** 一個市場完整跑完才換下一個。

**步驟 4 — 抽樣主迴圈（每市場湊滿「每市場配額」檔正式樣本）**

```
FOR 市場 IN [台股, 美股, 港股, 日股]:
    正式樣本 = 0
    WHILE 正式樣本 < 每市場配額:

        A. 候選池已空 → 記「候選池耗盡」→ 換下個市場

        B. 從 csv 抽 1 檔（依優先序），記住來自哪個 csv
           · 避開本輪已抽過的
           · 已在 Top 160 → 走 D（上輪 SOP-SYNC 漏刪）

        C. 立刻查最新收盤價、EPS、營益率、ROE（不沿用 csv 值）
           算 TTM PE = 最新收盤價 ÷ 近四季 EPS
           驗 G2/G3/G4：
             ├ 未過 → 跑 SOP-DEL → 回 A（不占額度）
             └ 通過 → 進步驟 5 驗 G1/G5/G6/G7
                        ├ 未過 → 跑 SOP-DEL → 回 A
                        └ 全過 → 進步驟 6 → 正式樣本 += 1

        D. 【榜上標的誤留 csv】（§2.4 同步遺漏，屬異常）：
           1) 補做 SOP-SYNC：把該列從 csv 刪掉並驗證
           2) 記一筆「二、修正紀錄」：<代號> 應於上輪同步刪列
           3) 不占額度、不重評（該檔交由 SOP-AUDIT 處理）→ 回 A 重抽

        E. 每累積 5 檔正式樣本 → 立刻存檔（避免中斷時整批遺失）
```

**一律拒絕並跑 SOP-DEL 的情況**：門檻任一項未達、EPS ≤ 0、數據缺漏/過期、口徑不明、來源衝突。
**不可硬湊**：候選池抽乾湊不滿配額時，不得用邊界值、過期值或外部標的遞補。

**重抽軌跡記錄**：`市場｜來源csv｜代號｜被哪道擋下｜資料日期｜拒絕原因｜已否從csv移除`。
結果檔只在「一、執行摘要」寫重抽次數與移除筆數，不污染 Top 160 表格。

**步驟 5 — 產業判定與剩餘門檻**

1. 判定產業：`一般企業`/`銀行`/`金控`/`保險`/`REIT`（決定 G5 指標）
2. 逐條查驗 G1、G5、G6、G7

| 結果 | 處理 |
|:---|:---|
| 七道全過 | 進步驟 6 |
| 任一道未過 | 跑 SOP-DEL → 回步驟 4 重抽 |
| 數據不足 | 視為未過，同上 |

**步驟 6 — 即時評分與個股撰寫（單檔閉環）**

七道全過後，**立刻做完這四件事再處理下一檔**：
1. 依 §6 公式算五維度分數與總分
2. 套不變式 I1~I7 自檢，過不了就重算
3. 依 §7 模板寫完個股 md ①~⑦
4. 更新主結果檔「三、Top 160 排名總覽」該列

正確：`A 評分 → 寫 A.md → B 評分 → 寫 B.md`
錯誤：`A 評分 → B 評分 → C 評分 → 最後才補寫 md`

**步驟 7 — 單市場存檔**

該市場配額跑完或候選池耗盡後存檔，體檢：
- [ ] 主結果檔 `## ` 大標題剛好 5 個（一~五）
- [ ] 子項分與總分算術無誤，通過不變式
- [ ] 總覽表格與個股 md 數據一致、排名不重複
- [ ] 本市場被拒標的**每一檔**都跑完 SOP-DEL

**步驟 8 — 市場推進**

額度未用完且還有市場 → 回步驟 4 換下一市場；額度用完 → 步驟 9。

---

### 階段三：收尾與自檢（步驟 9~10）

**步驟 9 — 排名整合、落榜清理、收工自查**

1. **去重**：同一公司多地上市時，只保留主要上市地原股。
2. **重排序與重命名**：依總分高到低排（同分依 §6.7 判定），更新名次，改個股 md 檔名前綴。
3. **落榜檔強制刪除** 🔴：目錄下凡不在 Top 160 的個股 `.md` 一律刪除。`0.StkScreenerResult_gemini.md` 與所有 `.csv` **不可刪**（csv 只能刪「列」不能刪「檔」）。
   - 🔴 **刪檔必同步刪列**：刪掉個股 md 的同時，一定要把總覽該列也移除。只刪檔不刪列 → 下輪開場會被 SOP-ORPHAN 當孤兒列清掉，等於把問題往後推一輪。
4. **完成度指標**（填入「一、執行摘要」）：
   - **A** = 總覽排名筆數
   - **B** = 目錄下 Top 160 個股 md 檔數
   - **C** = ①②③ 齊全的檔數
   - **D** = ⑤ 市佔具名合格的檔數
   - 合格：`B ≥ A − 10` 且 `C = B` 且 `D = B`。若 C < B 或 D < B → 當場補齊。
   - 🔴 跑完 SOP-ORPHAN 後正常情況應為 `A = B`。若收尾時 `B < A`，代表本輪又產生了孤兒列 → **當場補做 §2.5 SOP-ORPHAN**，不可留到下輪。
5. **三大門檻全樣本稽核**：逐檔複查正式樣本的 G2/G3/G4（含 `>10%` 這種沒實際數值的寫法），不合格的跑 SOP-DEL 重抽。
6. **🔴 SOP-SYNC：Top 160 同步刪列（本輪最後一次動 csv）**
   - 依 §2.4 七步驟，把**定案後 Top 160 的全部代號**從對應市場的 csv 刪除
   - 必須在「重排序與重命名」（第 2 點）之後做，確保用的是最終名單
   - 完成後重讀四個 csv 驗證：BOM／表頭完好、`原列數 − 刪除數 = 新列數`、Top 160 代號全部查無
   - 把「同步刪列 X 檔」與「各市場剩餘候選數」寫進「一、執行摘要」
7. **csv 刪列收尾稽核** 🔴：
   - 比對「被擋下的標的」與「已從 csv 移除的代號」→ 必須完全一致，有落差當場補做 SOP-DEL
   - 逐一抽查 Top 160 代號在對應 csv 是否確實查無，有殘留當場補做 SOP-SYNC
   - 重讀四個 csv，確認 BOM/表頭/格式與改動前一致
   - 把移除筆數（淘汰 ＋ 同步）與剩餘候選數寫進「一、執行摘要」
8. **市佔敘述稽核**：掃描禁用詞（見 §7.1），命中處當場改寫並記入「二、修正紀錄」。
9. **分批稽核標註**：在「五、下一輪待辦」中註明下輪稽核區間（本輪第 N 輪 → 下輪 N+1，第 8 輪後回到 1）。
10. **收工自查清單**（逐條打勾）：

**結構與檔名**
- [ ] 主結果檔 `## ` 大標題剛好 5 個（一~五）
- [ ] 無 `TOP01-...` 舊檔名殘留
- [ ] 目錄下無非 Top 160 的個股檔
- [ ] 總覽表格的排名、總分、公司名與個股 md 完全一致
- [ ] 總覽表格的公司欄連結都是 `[公司](<檔名.md>)` 格式且檔案存在
- [ ] 🔴 SOP-ORPHAN 已執行：總覽**每一列**都找得到對應個股 md（孤兒列 0 列）
- [ ] 本輪清掉的孤兒列，其代號已同步從對應市場 csv 移除
- [ ] 本輪刪掉的個股 md（落榜／稽核踢出），總覽對應列也都已移除（沒有製造新的孤兒列）

**抽樣與門檻**
- [ ] 所有正式樣本 PE 嚴格在 5.0~15.0 之間（不含邊界）
- [ ] 營益率、ROE、安全指標都寫了實際數值，無 `>10%` 搪塞
- [ ] 所有標的都來自 csv，無外部來源
- [ ] 門檻判定用即時查證數據，非 csv 舊值
- [ ] 被擋下的標的都已當場跑完 SOP-DEL

**稽核（SOP-AUDIT）**
- [ ] 本輪 20 檔稽核區間**每一檔**都重新查過最新數據（非只比對舊值）
- [ ] 每檔都重驗過 G1~G7、重算過五維度總分
- [ ] E1~E9 錯誤清單逐條跑完，命中處已當場修正
- [ ] 每檔 md 的「本區塊最後更新」與「本輪稽核」兩行已更新為今天
- [ ] 稽核踢出的標的：md 已刪、總覽已移除、名次已遞補重排
- [ ] 稽核造成的分數變動已同步到總覽表格

**csv 完整性**
- [ ] 被刪代號重讀 csv 確實查無；無另存/合併/新建 csv
- [ ] 被改動 csv 的 BOM/表頭/引號風格/編碼/換行與改動前一致
- [ ] 港股前導 0（`00151`）與日股千分位（`"1,861"`）寫回後未被破壞
- [ ] 無「判定不合格但還留在 csv」的標的
- [ ] 🔴 SOP-SYNC 已於**本輪收尾**執行（不因「上輪刪過」而略過）：**Top 160 全部 160 個代號在對應 csv 一律查無**
- [ ] 刪列只跨對應市場的 csv（台股代號沒去刪到 jp.csv，反之亦然）
- [ ] 四個 csv 都還在（只刪列，未刪檔、未清空、未新增列）

**內容品質**
- [ ] 每檔 ⑦ 五維度相加 = 標示總分
- [ ] 全數通過不變式 I1~I7
- [ ] 禁用詞掃描 0 處殘留
- [ ] 「五、下一輪待辦」已填
- [ ] Chat 只回精簡摘要

11. **填「五、下一輪待辦」**（每輪必填，不得留空）。

**步驟 10 — 最終存檔與回報**

1. 寫入 `StkScreenerResult\0.StkScreenerResult_gemini.md`。
2. 再讀一次驗證：日期當天、區塊沒變少、`## ` 大標題 5 個、個股檔連結有效。
3. **最後一次 csv 驗證** 🔴：重讀四個 csv，確認 ①四檔都在 ②BOM/表頭/引號風格完好 ③Top 160 代號全部查無（SOP-SYNC 已生效）。
4. **對話框只輸出精簡摘要（§9 格式）**，嚴禁貼完整檔案內容或大表格。

---

## 6. 五維度評分系統（滿分 100）

### 通用規則

```
clamp(min, max, x) = max(min, min(max, x))

計算順序：子項分（小數 2 位）→ 各維度分（四捨五入到小數 1 位）→ 總分 = 5 維度直接相加
```

| 維度 | 配分 |
|:---|:-:|
| 一、市場地位 | 25 |
| 二、成長性 | 25 |
| 三、獲利品質 | 20 |
| 四、估值 | 20 |
| 五、財務安全 | 10 |
| **合計** | **100** |

### 6.1 一、市場地位（25 分）

```
子項1  名次基準分（三選一）：第 1 名 = 18.0 ｜第 2 名 = 14.0 ｜第 3 名 = 10.0
子項2  市佔率加成：           min(4.0, 市佔率% × 0.1)
子項3  護城河加分：           每具備一項 +1.0，上限 3.0 → 只能是 0/1/2/3 的整數
                              （高轉換成本 / 網路效應 / 規模經濟 / 特許獨佔）
維度分 = clamp(0, 25.0, 子項1 + 子項2 + 子項3)
```

🔴 計分前提：
- 名次與市佔率必須來自 ⑤-1 的已具名資料。只寫「龍頭」「領先」無具名數據 → 子項 1、2 計 0 分。
- 護城河每加 1 分須在 ⑤-3 附量化證據，不得給 0.5 分。

### 6.2 二、成長性（25 分）

```
子項1  5 年淨利 CAGR：  clamp(0, 15.0, 5年CAGR% × 0.75)
子項2  10 年淨利 CAGR： clamp(0, 10.0, 10年CAGR% × 0.50)
維度分 = 子項1 + 子項2
```

> 無 10 年數據但有 ≥ 7 年可替代（須標註）；< 7 年 → 子項 2 計 0 分。

### 6.3 三、獲利品質（20 分）

```
OPM% = (營業利益率 ÷ 稅前淨利率) × 100%

子項1  OPM 分數：       max(0, 8.0 − |OPM% − 100| × 0.1)
子項2  營業利益率分數： min(6.0, 營業利益率% × 0.20)
子項3  5 年 ROE 中位數：clamp(0, 6.0, 5年ROE中位數% × 0.30)
維度分 = 子項1 + 子項2 + 子項3
```

> OPM 愈接近 100% 分數愈高（代表營業外損益干擾小）。

### 6.4 四、估值（20 分）

```
子項1  PE 分數：          clamp(0, 12.0, 12.0 × (15.0 − PE) ÷ 10.0)
子項2  5 年殖利率中位數： min(8.0, 5年殖利率中位數% × 1.60)
維度分 = 子項1 + 子項2
```

> 因 G2 規定 PE 嚴格在 (5, 15) 開區間，子項 1 合法值必在 (0, 12.0)。算出 ≥ 12.0 代表 PE ≤ 5.0，該檔應被 G2 擋掉 → 跑 SOP-DEL。

### 6.5 五、財務安全（10 分，依產業五選一）

```
一般企業： clamp(0, 10.0, 10.0 × (70.0 − 負債比%) ÷ 70.0)
銀行業：   clamp(0, 10.0, (CAR% − 10.5) × 2.0)
金控業：   clamp(0, 10.0, (集團適足率% − 100) ÷ 5.0)
保險業：   clamp(0, 10.0, (RBC% − 200) ÷ 15.0)
REIT：     clamp(0, 10.0, 10.0 × (50.0 − LTV%) ÷ 50.0)
```

### 6.6 缺失值與特殊情況

| 情況 | 處理 |
|:---|:---|
| 子項查無公開數據 | 計 **0 分**，標 `資料缺漏`，嚴禁臆測 |
| 稅前淨利率 ≤ 0 | OPM 子項計 0 分，標 `OPM 不適用` |
| 引用財務絕對金額 | 標來源日期，**強制換算每股金額** |
| 財務安全標註 | 寫明公式類型與代入值，如 `財務安全 6.2（REIT／LTV 19.0%）` |

### 6.7 同分排序

總分相同時依序：① PE 較低 → ② 5 年淨利 CAGR 較高 → ③ 財務安全較高 → ④ 代號升冪。

### 6.8 不變式檢核（評分與稽核時逐條套）

任一條違反 = 算術錯誤，當場重算並記入「二、修正紀錄」。

| # | 不變式 | 抓什麼 |
|:-:|:---|:---|
| **I1** | 市場地位 = `{18/14/10}` + `min(4, 市佔%×0.1)` + k，**k ∈ {0,1,2,3} 整數** | 護城河只能加整數，出現 23.5 必錯 |
| **I2** | 各維度 ≤ 配分上限（25/25/20/20/10） | 子項加總溢位 |
| **I3** | 估值子項 1 在 (0, 12.0) 開區間 | ≥ 12 → PE ≤ 5 → 應被 G2 擋 |
| **I4** | 總分 = 五維度（各已四捨五入到 1 位）直接相加 | ⑦ 驗算列必須等於五項相加 |
| **I5** | 若 ⑤-1 標 `⚠️ 市佔資料不足` → 市場地位 ≤ 3.0 | 沒具名卻拿 20 幾分 |
| **I6** | 總覽 PE/營益率/安全指標值三欄必須是具體數值 | 抓 `>10%` 或填錯欄位 |
| **I7** | 總覽該列五維度與總分和個股 md ⑦ 逐字相同 | 兩處不同步 |

---

## 7. 個股詳細檔撰寫規格

檔案路徑：`StkScreenerResult\<排名>-<代號>-<公司>.md`
必須包含 **① 至 ⑦**（⑤ 含 ⑤-1、⑤-2、⑤-3 三張必填子表）。

````markdown
### 第 N 名：<代號> <公司名稱> — 總分 XX.X / 100（<市場>／<產業別>）
- **本區塊最後更新**：YYYY-MM-DD
- **本輪稽核**：YYYY-MM-DD｜G1~G7 重驗 ✅｜總分 XX.X → XX.X｜本輪補充：<新財報/新市佔/新事件，沒有就寫「無新增，數據沿用 YYYY-MM-DD 來源並已複查時效」>
  （新入榜檔第一次寫入時填 `新入榜（YYYY-MM-DD 首次分析）`，之後每次跑 SOP-AUDIT 都要覆寫這行）
- **一句話定位**：<點名「什麼產品/服務」在「什麼市場」排第幾，100~500 字>

**① 為什麼能進 Top 160**（必列 3 點，每點 = 維度 + 分數 + 具名產品市場）
1. <例：市場地位 24.0/25 — 中國海域原油產量第 1、市佔 57.0%>
2. <例：成長性 21.4/25，5 年淨利 CAGR 達 22.5%>
3. <例：獲利品質 19.2/20，營益率 38.5% 且 OPM 95.8%>

**② 優點（買它的理由）**（3~5 點，每點 = 事實 + 數據 + 來源日期）
| # | 優點 | 具體數字 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**③ 缺點與風險（不買它的理由）**（3~5 點，嚴禁「無明顯風險」）
| # | 缺點 / 風險 | 具體數字或事件 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**④ 什麼情況會被踢出榜單**（2~3 條量化觸發條件）
- <例：TTM PE 升破 15.0（目前 X.X，尚有 XX% 緩衝）>

**⑤ 硬性門檻查驗**（🔴 五項都要寫實際數值，禁止 `>10%`）：
PE X.XX ✅｜營業利益率 XX.X%（年度 XX.X% / TTM XX.X%）✅｜ROE XX.X%（5 年中位數 XX.X%）✅｜<安全指標> XX.X% ✅｜非 ADR 普通股・非 OTC（<交易所>）✅

**⑤-1 市佔定位（🔴 六欄缺一即 `資料缺漏`）**
| 主力產品/服務 | 市場範圍 | 排名 | 市佔率 | 統計口徑 | 來源/日期 |
|:---|:---|:-:|:-:|:---|:---|
| <具體產品名，不可寫「本業」> | <全球/區域/國家> | 第X名 | XX.X% | <營收/出貨量等> | <機構, YYYY-MM-DD> |

**⑤-2 競爭格局（🔴 必填）**
| 排名 | 公司 | 市佔率 | 與本公司差距 |
|:-:|:---|:-:|:---|
| 1 | ... | XX.X% | ... |

**⑤-3 為什麼能排在這個名次**（2~4 點 = 可驗證機制 + 量化證據，不可只寫「品牌強」）
- <例：成本優勢 — 桶油成本 28.5 美元，低於 Shell 34.2、PetroChina 41.0（各公司 2025 年報）>

**⑥ 財務數據**：最新 EPS <金額/每股>｜預估 EPS｜5 年 ROE 中位數 XX.X%｜5 年殖利率中位數 XX.X%｜OPM XX.X%｜5 年淨利 CAGR XX.X%｜10 年淨利 CAGR XX.X%
（各項附來源日期；REIT 另附股價淨值比、租金報酬率、空租率）

**⑦ 分數拆解**：市場地位 XX.X（名次 XX.XX + 市佔 XX.XX + 護城河 X）｜成長性 XX.X（5年 XX.XX + 10年 XX.XX）｜獲利品質 XX.X（OPM XX.XX + 營益率 XX.XX + ROE XX.XX）｜估值 XX.X（PE XX.XX + 殖利率 XX.XX）｜財務安全 XX.X（<產業別>／<指標值>）
- **總分驗算**：XX.X + XX.X + XX.X + XX.X + XX.X = **XX.X**
- **不變式自檢**：I1 ✅｜I2 ✅｜I3 ✅｜I4 ✅｜I5 ✅｜I6 ✅
````

### 7.1 市佔敘述具名規範

**核心要求**：提到公司地位必須回答六個問題 — **哪個產品**、**哪個市場**、**第幾名**、**市佔多少%**、**贏過誰**、**憑什麼**。只寫「龍頭」不回答 → 視為未完成。

**禁用詞**（單獨出現即不合格）：`龍頭`、`領先`、`領頭`、`卓越`、`居前列`、`名列前茅`、`優秀標的`、`行業代表`、`具備高度行業競爭優勢`、`市佔率居區域前列`、`市場獨佔或領頭地位`。
> 例外：上述詞若**緊接**具體「產品 + 市場 + 名次 + 市佔率 + 來源」可保留作修飾語。

**改寫範例**：
| ❌ 不合格 | ✅ 合格 |
|:---|:---|
| 港股該行業龍頭代表標的，市場地位卓越。 | 中國海域原油產量排名第 1、市佔 57.0%（2025 年產量口徑，公司年報 2026-03-26）；第 2 名中石化 21.0%、第 3 名中石油 18.0%。 |

**多角化公司**：就營收占比最高的前 2 項業務分別列出名次與市佔率。
**查無第三方市佔**：⑤-1 標 `⚠️ 市佔資料不足`，子項 1、2 計 0 分，記入「四、待查」。

---

## 8. 主結果檔標準骨架

`StkScreenerResult\0.StkScreenerResult_gemini.md` **必須且只能包含以下 5 個 `## ` 大標題**（一~五），不可增減。個股詳細分析一律寫入獨立的 `<排名>-<代號>-<公司>.md`，主結果檔在「三、Top 160 排名總覽」表格的公司欄位直接以 Markdown 連結對應各個股檔（如 `[公司名稱](<檔名.md>)`）。

````markdown
# 全市場選股篩選結果（Gemini）
> 更新日期：YYYY-MM-DD hh:mm:ss
> 篩選條件：市佔前3 ∪ 產品市佔前3、嚴格 5<PE<15（不含邊界）、營業利益率>10%、ROE>10%、財務安全（一般 負債比<70%／銀行 CAR≥10.5%／金控 集團適足率≥100%／保險 RBC≥200%／REIT LTV<50%）、排除普通股 ADR 與 OTC（ADR 特別股不排除）

## 一、執行摘要
- **抽樣母體（csv）**：讀入 X 個 csv｜台 X／美 X／港 X／日 X｜市場不明 X
- **本次抽樣**：台 X／美 X／港 X／日 X（合計 X / <本輪額度>；僅計正式樣本）｜新入榜 X
- **🔴 榜單孤兒列清理（SOP-ORPHAN）**：本輪清除 X 列（代號：…；md 不存在＝已移出 Top 160）｜同步 csv 刪列 X 檔（台 X／美 X／港 X／日 X）｜清理後總覽 X 列
- **csv 淘汰刪列**：本輪移除 X 檔（台 X／美 X／港 X／日 X）
- **🔴 csv 榜單同步刪列（SOP-SYNC）**：本輪同步移除 X 檔（台 X／美 X／港 X／日 X）｜Top 160 代號殘留 `✅ 0 檔`｜刪列後剩餘 台 X／美 X／港 X／日 X
- **初篩重抽紀錄**：台 X／美 X／港 X／日 X 次
- **Checkpoint 完成度**：`✅台 ✅美 ⬜港 ⬜日`（第 X 輪）
- **本次額度使用**：補說明 Z｜初篩 X｜門檻通過 Y｜深度分析 Y（合計 ≤ 本輪額度）
- **結果**：新入榜 X｜落榜 X｜修正 X 處
- **Top 160 門檻分數**：XX.X 分
- **榜單**：X / 160 檔
- **說明完成度**：A=XX　B=XX　C=XX　D=XX
- **市佔敘述改寫**：本輪改寫 X 處（禁用詞：`✅ 0 處` / `⚠️ X 處`）
- **🔴 稽核重新分析（SOP-AUDIT）**：完成 TOP XX~XX（第 N/8 輪）共 X 檔全部重查重評｜稽核維持 X｜稽核重評 X（分數變動 ≥3.0）｜稽核踢出 X｜補充新資料 X 檔｜稽核修正 X 處（E1~E9）｜不變式違反 X 處

## 二、修正紀錄 (Self-Fix Log)（只保留最新 5 筆）
| # | 標的 | 錯誤內容 | 修正後 | 依據來源 | 修正日期 |
|:-:|:--|:--|:--|:--|:--|

## 三、Top 160 排名總覽
> 「市佔定位」格式：`<產品>｜<市場>｜第 X 名｜XX.X%`，嚴禁「龍頭」「居前列」。
> 🔴 「公司」欄位直接連結至對應個股詳細檔（如 `[中國海洋石油](01-0883-中國海洋石油.md)`）。
> 🔴 最後三欄填具體數值：PE 填 X.XX；營益率填 XX.X%（禁 `>10%`）；安全指標值填 `<指標名> XX.X%`（如 `負債比 38.2%`）。

| 排名 | 代號 | 公司 | 市場 | 產業別 | 市佔定位（產品｜市場｜名次｜市佔率） | 市場地位 (25) | 成長性 (25) | 獲利品質 (20) | 估值 (20) | 財務安全 (10) | 總分 | PE | 營益率 | 安全指標值 |
|:-:|:--|:--|:-:|:-:|:--|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:--|
| 1 | 0883 | [中國海洋石油](01-0883-中國海洋石油.md) | 港股 | 一般 | 海域原油與天然氣開採｜中國海域｜第 1 名｜57.0% | 24.0 | 23.5 | 17.8 | 11.4 | 5.4 | 82.1 | 8.00 | 52.5% | 負債比 32.2% |

## 四、⚠️ 待查 / 資料不足清單
| 代號 | 公司 | 市場 | 卡關項目 | 已試過的來源 | 推測原因 |
|:--|:--|:-:|:--|:--|:--|

## 五、下一輪待辦（🔴 每輪必填）
- **待補個股說明**（最優先）：（代號 + 名次）
- **下一輪稽核重分析區間**：（如 TOP 21~40，第 N/8 輪）
- **待改寫的空泛市佔敘述**：（代號 + 名次 + 目前寫法）
- **待修正的不變式違反**：（代號 + 違反哪一條）
- **csv 候選池狀況**：剩餘 台X／美X／港X／日X（皆為未入榜標的）
- **待刪 csv 列（上輪寫入失敗者）**：（csv 檔名 + 代號 + 屬 SOP-DEL／SOP-SYNC／SOP-ORPHAN）
- **下一輪要做的工作**：…
- **未跑完的市場**：…
````

---

## 9. 對話框回覆格式

每輪執行完，Chat **只能**回傳以下精簡摘要，**嚴禁貼完整 Markdown 或大表格**：

```text
✅ StkScreener(gemini) — YYYY-MM-DD_hh:mm:ss
- 完成市場：台/美/港/日（Checkpoint ✅✅✅✅）
- 孤兒列清理：總覽 X 列查無個股 md → 已刪列 ＋ csv 同步刪 X 檔 ✅（清理後總覽 X 列）
- 稽核重分析：TOP XX~XX（第 N/8 輪）X 檔全部重查重評 ✅
  └ 維持 X｜重評 X（分數變動 ≥3.0）｜踢出 X｜補新資料 X 檔｜E1~E9 修正 X 處
- csv 候選池：讀入 X 個 csv｜淘汰刪列 X 檔｜榜單同步刪列 X 檔｜剩餘 台X/美X/港X/日X
- csv 刪列稽核：被擋下 X 檔 = 已刪列 X 檔 ✅｜Top 160 代號殘留 0 檔 ✅（BOM/格式驗證通過）
- 額度使用：補說明 X + 新分析 Y（合計 Z / <本輪額度>）
- 初篩稽核：正式樣本 Z / <本輪額度> 皆通過七道門檻 ✅｜重抽 X 次
- 結果：新入榜 X｜落榜 X｜修正 X 處
- 不變式：I1~I7 違反 0 處 ✅（本輪修正 X 處）
- 榜單：X / 160（門檻分數 XX.X）
- 說明完成度：A=XX　B=XX　C=XX　D=XX
- 市佔具名：禁用詞殘留 0 處 ✅｜改寫 X 處
- 下一輪待辦：…
```
