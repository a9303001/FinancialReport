/goal
# 全市場選股篩選（Stock Screener · Gemini 版）

> **這是執行規格書。每次執行前，從第 1 章讀到最後一章，再開始動手。**
> 全檔用繁體中文（台灣用語）。專有名詞第一次出現時附英文，例如 `營業利益率（Operating Margin）`。

---

## 1. 三十秒看懂這個任務

| 項目 | 說明 |
|:---|:---|
| **目標** | 維護一份 Top 160 股票榜單 |
| **每輪要做的事** | ①清孤兒列 ②重新分析 20 檔榜上股 ③抽 20 檔新標的驗證評分 ④重排榜單 ⑤同步刪 csv |
| **標的從哪來** | 只能從 `StkScreenerResult\*.csv` 抽。禁止自己想、禁止用網路排行榜、禁止用記憶中的名單 |
| **怎麼篩** | 七道門檻 G1~G7，全部通過才可以評分 |
| **怎麼評分** | 五個維度合計 100 分 |
| **產出什麼** | 每檔一個 `<排名>-<代號>-<公司>.md`，外加一個主結果檔 |
| **沒過門檻怎麼辦** | 立刻把那一列從 csv 刪掉，然後重抽（＝程序 A） |

### 1.1 檔案路徑

| 項目 | 路徑 |
|:---|:---|
| 規格檔（就是本檔） | `Routines_StkScreener_gemini.md` |
| 主結果檔 | `StkScreenerResult\0.StkScreenerResult_gemini.md` |
| 個股詳細檔 | `StkScreenerResult\<排名>-<代號>-<公司>.md` |
| 抽樣母體 | `StkScreenerResult\*.csv`（該目錄下全部 csv） |

### 1.2 個股檔命名規則

第 1~99 名，排名補零到 **2 位**；第 100~160 名，排名用 **3 位**（不用補零）。

- ✅ 正確：`01-0883-中國海洋石油.md`、`57-MET-MetLife.md`、`100-OXY-Occidental Petroleum.md`、`160-1264-德麥.md`
- ❌ 錯誤：`TOP01-...`、`1-...`、`001-...`、`0883-中國海洋石油.md`

### 1.3 三支 csv 刪列程序（用中文名記，代號只是簡稱）

| 簡稱 | 中文名 | 什麼時候做 | 刪什麼 |
|:---|:---|:---|:---|
| **程序 A（SOP-DEL）** | 淘汰刪列 | 抽樣當下，某檔沒過門檻時 | 只刪 csv 那一列 |
| **程序 B（SOP-ORPHAN）** | 孤兒列清理 | 每輪**開場** | 刪總覽那一列 ＋ 刪 csv 那一列 |
| **程序 C（SOP-SYNC）** | 榜單同步刪列 | 每輪**收尾** | 把 Top 160 全部代號從 csv 刪掉 |

詳細做法在第 4 章。三支程序共用同一套「csv 安全讀寫規則」（§4.1）。

### 1.4 其他名詞

| 名詞 | 意思 |
|:---|:---|
| **孤兒列** | 總覽表格裡有這一列，但它連結的個股 md 檔**不存在**。代表這檔股票已經被移出 Top 160 |
| **稽核（SOP-AUDIT）** | 每輪對 20 檔榜上股**重新做一次完整分析**：重查數據 → 重驗門檻 → 重算分數 → 抓錯 → 補新資料 |
| **G1~G7** | 七道硬性門檻，全過才能評分（第 5 章） |
| **I1~I7** | 七條不變式，用來檢查算術有沒有錯（§6.7） |
| **E1~E9** | 稽核時要逐條檢查的九種錯誤（§7.3） |
| **三大門檻** | G2（PE）、G3（營益率）、G4（ROE）。抽樣當下先驗這三道，比較省時間 |
| **本輪額度** | 每輪總共處理幾檔**新標的**＝ 20 檔。稽核的 20 檔不算在裡面 |
| **每市場配額** | 每個市場要湊滿幾檔正式樣本＝ 5 檔 |
| **正式樣本** | 已經通過七道門檻、佔用本輪額度的標的 |
| **候選池** | csv 裡面**還沒入榜**的標的。入榜就會被程序 C 刪掉，所以 csv 和 Top 160 互斥 |
| **說明缺口** | 榜上有名次，但個股 md 不存在、內容殘缺、或市佔沒具名的檔數 |

---

## 2. 十條鐵則（違反任何一條就算本輪失敗）

1. **標的只能從 csv 抽。** 不可以自己想標的，不可以用網路排行榜，不可以用記憶中的名單。
2. **門檻判定只用即時查到的最新數據。** csv 裡的數字只能決定「先抽誰」，不能當作通過依據，也不可以抄進個股 md。
3. **七道門檻任一道沒過，立刻跑程序 A 刪列，再重抽。** 不可以留到最後一起處理。
4. **一次只處理一檔。** 這一檔評分完、md 寫完，才可以碰下一檔。禁止先全部評分、最後才補寫 md。
5. **csv 只能刪「列」，不能刪「檔」。** 不可以清空、不可以合併、不可以新增列、不可以另存新檔。
6. **G3、G4 禁止寫 `>10%` 交差。** 一定要寫到小數點，總覽和個股 md 兩邊都要寫。
7. **提到公司地位一定要具名。** 產品、市場、名次、市佔率、贏過誰、憑什麼，六項都要有（§8.2）。
8. **每輪開場一定跑程序 B，每輪收尾一定跑程序 C。** 不可以因為「上輪做過了」就略過。
9. **稽核一定要重新查證最新數據。** 只比對舊數字就宣告通過＝這一步沒做。
10. **對話框只回精簡摘要（第 10 章）。** 禁止貼完整檔案內容或大表格。

---

## 3. 執行參數

| 參數 | 值 | 說明 |
|:---|:---|:---|
| **語言** | 繁體中文 | 專有名詞首次出現附英文 |
| **抽樣母體** | `StkScreenerResult\*.csv` | 唯一來源 |
| **🔧 每輪總額度** | **20 檔** | 補說明 ＋ 新抽合計上限（4 市場 × 每市場配額）。要改數量只改這格與下格 |
| **🔧 每市場配額** | **5 檔正式樣本** | 被拒絕的標的不佔配額 |
| **🔧 每輪稽核批量** | **20 檔（8 輪走完 Top 160）** | 不佔本輪額度，而且要**先做稽核再抽新股** |
| **市場順序** | 台股 → 美股 → 港股 → 日股 | 一個市場完整做完才換下一個 |
| **榜單容量** | Top 160 | 依總分由高到低。不足 160 檔就照實列，嚴禁湊數 |
| **🔧 擴充期補位** | 榜單不足 160 檔時，每輪額度全部拿去抽新股 | 目前榜單正從 Top 100 擴充到 Top 160。**不可以**因為「榜單沒滿」就放寬 G1~G7 |
| **計算精度** | 子項 2 位小數 → 維度 1 位小數 → 相加 | 子項算到小數第 2 位；維度四捨五入到第 1 位；總分＝五個維度直接相加 |
| **時間格式** | `YYYY-MM-DD_hh:mm:ss` | 結果檔與摘要的時間戳記 |

---

## 4. csv 刪列程序（程序 A / B / C）

### 4.1 csv 安全讀寫規則（三支程序共用，最重要）

🔴 **唯一正確做法**：把 csv 當**純文字**整份讀進來 → 逐行比對第一欄代號 → 移除命中的那幾行 → 其餘行**原封不動**寫回原路徑。

🔴 **絕對不要**「解析成表格再重新序列化」。那是 BOM 消失、引號跑掉、千分位被破壞的最大原因。

**代號比對規則**：

- 去掉引號與 BOM 之後做**完全比對**。
- 港股要保留前導 0：`00836` ≠ `836`。
- 美股大小寫不敏感，但必須全等，不可用前綴比對：`CF` ≠ `CFG`。
- 只比對**該市場對應的 csv**。台股 `1301` 和日股 `1301` 是不同公司，跨市場比對會誤刪。

**各 csv 原始格式（供參考，每輪仍以實際讀到的為準）**：

| csv | 編碼 | 表頭第一欄 | 引號 | 注意 |
|:---|:---|:---|:---|:---|
| `TW.csv` | UTF-8 with BOM | `代號` | 無引號 | 末欄「主要業務」是長文字 |
| `US.csv` | UTF-8 with BOM | `代碼` | 無引號 | 代號是英文字母 |
| `hk.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 代號 5 位含前導 0，不可被吃掉 |
| `jp.csv` | UTF-8 with BOM | `"代碼"` | 每欄加雙引號 | 數值含千分位逗號如 `"1,861"`，靠引號保護 |

**每次寫回後都要重讀驗證**：BOM 還在、表頭完好、`原列數 − 刪除數 = 新列數`、被刪代號查無。

**寫入失敗時**：門檻判定與總覽刪列照樣有效（不會因為 csv 寫不進去就改判）。把「待刪列」記進「五、下一輪待辦」的「待刪 csv 列」，下輪開場第一件事就是補刪。

### 4.2 程序 A：淘汰刪列（SOP-DEL）

**什麼時候做**：七道門檻任一道沒過。包含查不到、無法驗證、口徑不明、來源衝突、剛好等於邊界值。

**唯一例外**：「市場歸屬判不出來」不算不合格 → 該列留在 csv，記進「四、待查」。

**五個動作（做完才准抽下一檔）**：

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 確認這一檔來自**哪一個 csv** | 抽樣當下就要記住 |
| 2 | 用**股票代號完全比對**找到那一列 | 不可用公司名模糊比對、不可依順序刪、不可整批清空 |
| 3 | 只刪這一列 | 其他列一個字元都不准動 |
| 4 | 寫回它原本那個 csv 的原路徑 | 不可另存新檔、不可搬到別的 csv |
| 5 | 重讀驗證（依 §4.1） | 記一筆 `<代號> 從 <csv> 移除 — 卡在 G<X>` 進「二、修正紀錄」 |

⏱ **順序**：`判定不合格 → 刪列 → 存回 → 驗證 → 才准抽下一檔`。不准把多檔合併起來一次刪。

### 4.3 程序 B：孤兒列清理（SOP-ORPHAN，每輪開場必做）

**核心規則**：總覽的**每一列**都必須有對應的個股 md 檔。**找不到檔＝這檔股票已被移出 Top 160** → 刪掉總覽那一列，並把該代號從對應市場的 csv 刪掉。

**什麼時候做**：讀完主結果檔與所有 csv 之後、跑稽核之前。

**為什麼要刪**：個股 md 只會因為「稽核踢出」或「落榜清理」而消失。檔沒了卻還留著總覽列，代表上輪只刪檔、忘了刪列。留著會讓總覽連結指向不存在的檔。

**執行步驟**：

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 逐列取出總覽的「排名／代號／公司／市場／連結檔名」 | 一列都不可略過，含最後一名 |
| 2 | 檢查連結檔名在 `StkScreenerResult\` 底下**是否真的存在** | 🔴 以**檔案系統實際存在**為準，不是看連結文字長得對不對 |
| 3 | 檔案存在 → 保留該列，什麼都不用做 | 正常情況全部命中 |
| 4 | 檔案不存在（＝孤兒列）→ **直接刪掉總覽那一列** | 🔴 不重新分析、不補寫 md、不試圖救回 |
| 5 | 依該列「市場」欄找到對應 csv，用代號完全比對刪列 | 比對與寫回完全照 §4.1 |
| 6 | 每刪一檔記一筆「二、修正紀錄」 | 格式：`<代號> 個股 md 不存在（已移出 Top 160）→ 自總覽刪列，並自 <csv> 移除` |
| 7 | 清理後**不用馬上重排名次** | 名次暫時不連號可以接受，最後由步驟 9 統一重排。但本輪稽核區間要用**清理後由上而下的順位**計算 |
| 8 | 把「孤兒列清理 X 列／同步 csv 刪列 X 檔／清理後總覽 X 列」寫進「一、執行摘要」 | 見第 9 章骨架 |

**邊界情況**：

| 情況 | 怎麼處理 |
|:---|:---|
| 檔案在，只是**排名前綴對不上**（md 是 `12-2496-卓越.md`，連結寫 `11-2496-卓越.md`） | 🔴 **不是孤兒列**。用代號 ＋ 公司名找得到檔就算存在 → 屬 **E8 檔名／連結錯**，改連結或依 §1.2 更名。**嚴禁刪列** |
| 檔案在，只是公司名寫法有差（全形括號、空白、繁簡） | 同上，當作 E8 修正，不刪列 |
| 檔名含**空白或括號**（如 `53-00316-東方海外國際 (OOIL).md`） | 🔴 **最常見的誤判來源**。用正則抓 `[公司](檔名)` 時，檔名裡的 `)` 會讓連結被提早截斷，好檔被誤判成孤兒。務必拿**完整檔名字串**去檢查檔案存在，確認真的不存在才判孤兒列 |
| 總覽列**連連結都沒有**（純文字公司名） | 用代號去目錄找 `*-<代號>-*.md`：找得到 → 補上連結（E8）；找不到 → 認定孤兒列，照第 4~6 步刪 |
| 代號在對應 csv 裡找不到 | 正常（入榜那輪已刪過），不算錯誤，不用記錄 |
| 該列「市場」欄是「市場不明」 | 總覽列照刪；csv 刪列記進「五、下一輪待辦」，補完市場歸屬後下輪再刪 |
| 清理後榜單不足 160 檔 | **正常**。空缺由本輪或後續輪次的新抽樣自然補上。🔴 嚴禁為了湊滿 160 檔而放寬門檻或保留孤兒列 |

🔴 **絕對禁止**：為了讓總覽維持 160 列而保留沒有 md 的列；或幫孤兒列臨時補寫一個 md 讓它「復活」。**md 不存在就是已退出榜單，沒有復活機制。**

### 4.4 程序 C：榜單同步刪列（SOP-SYNC，每輪收尾必做）

**核心規則**：**csv 是「還沒入榜的候選池」。Top 160 榜單上的標的，一律不可以留在任何 csv。**

**什麼時候做**：步驟 9 排名整合完成、Top 160 名單定案之後。**這是本輪最後一次動 csv。**

🔴 **無條件執行**：就算本輪沒有新入榜、沒有稽核踢出、榜單一列都沒變，仍然要拿**定案後 Top 160 的全部代號**重跑一次刪列比對。**不可以**因為「上輪已經刪過了」就略過。上輪漏刪、使用者手動補列、同代號重複列，都只能靠這一步抓出來。

**為什麼要刪**：

| 理由 | 說明 |
|:---|:---|
| 避免重複抽樣 | 榜上標的留在 csv 會被重複抽中，白白吃掉本輪額度 |
| 候選池收斂 | csv 剩餘數才會等於「真正還可以抽的數量」 |
| 職責分離 | 榜上標的改由稽核負責維護，不靠抽樣碰運氣 |

**執行步驟**：

| 步驟 | 動作 | 要求 |
|:-:|:---|:---|
| 1 | 取定案後 **Top 160 全部代號**（不是只有本輪新入榜的） | 以「三、Top 160 排名總覽」為準 |
| 2 | 依「市場」欄把代號分成台／美／港／日四組 | |
| 3 | 每組**只比對該市場的 csv** | 🔴 跨市場比對會誤刪 |
| 4 | 依 §4.1 讀檔、完全比對、移除命中行、原樣寫回 | |
| 5 | 重讀四個 csv 驗證 | BOM 還在、表頭完好、列數對得上、Top 160 代號全部查無 |
| 6 | 把「同步刪列 X 檔／各市場剩餘候選數」寫進「一、執行摘要」 | |

**邊界情況**：

| 情況 | 怎麼處理 |
|:---|:---|
| 代號在 csv 裡找不到 | 正常，不算錯誤，不用記錄 |
| 同一個 csv 有多列同代號 | 全部刪掉，並記進「二、修正紀錄」 |
| 本輪落榜／被稽核踢出的標的 | 🔴 **不回填 csv**。它們早在入榜那輪就被刪了，落榜後就退出候選池，沒有復活機制 |
| 榜單市場欄是「市場不明」 | 不刪，記進「四、待查」，補完市場歸屬後下輪再刪 |
| csv 寫入失敗 | 依 §4.1 最後一段處理 |

🔴 **絕對禁止**：刪掉整個 csv 檔、清空 csv、把多個 csv 合併、為了「湊剩餘數」而新增列。

---

## 5. 七道硬性門檻 G1~G7

🔴 **任一道沒過 → 立刻跑程序 A（§4.2）。**

| # | 門檻 | 判定標準 | 沒達標怎麼辦 |
|:-:|:---|:---|:---|
| **G1** | **市佔前 3 名（必須具名）** | 產業整體**或**核心主力產品市佔前 3 名，要引述第三方數據（Gartner／IDC／Statista／Euromonitor／產業公會／年報，公關宣傳不採信）。🔴 六項全部要寫出來：①產品名 ②市場範圍 ③名次 ④市佔率% ⑤第 1~3 名競爭對手及其市佔率 ⑥來源機構與日期。缺任一項就算沒過 | 剔除。若「產業判得出來但查無第三方市佔」→ 標 `⚠️ 市佔資料不足`，記進「四、待查」 |
| **G2** | **PE 估值區間** | `5.0 < TTM PE < 15.0`（嚴格不含邊界）。TTM PE ＝ 最新收盤價 ÷ 近四季 EPS。EPS ≤ 0 視為不符 | PE ≤ 5.0、≥ 15.0 或無法驗證 → 剔除 |
| **G3** | **營業利益率** | 最近完整年度 **> 10.0%** 且 TTM **> 10.0%**（兩個都要達標，**兩個都要寫實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G4** | **ROE** | 5 年中位數 **> 10.0%** 且 最新/TTM **> 10.0%**（兩個都要達標，**兩個都要寫實際數值**） | 任一項 ≤ 10.0% → 剔除 |
| **G5** | **財務安全** | 依產業套用專屬指標（見下表） | 沒達標 → 剔除；查不到 → 標 `資料缺漏`、財務安全計 0 分，記進「四、待查」 |
| **G6** | **排除 ADR 普通股** | ADR **特別股不排除** | ADR 普通股 → 剔除 |
| **G7** | **排除 OTC** | 必須在正規交易所掛牌（NYSE／NASDAQ／TWSE／TPEx／HKEX／TSE 等） | OTC／Pink Sheets → 剔除 |

> G2／G3／G4 合稱**三大門檻**，抽樣當下先驗這三道以省時間。

### 5.1 G5 財務安全指標（依產業五選一）

| 產業別 | 指標 | 門檻 |
|:---|:---|:---|
| 一般 | 負債比率 ＝ 總負債 ÷ 總資產 | **< 70.0%** |
| 銀行 | 資本適足率（CAR） | **≥ 10.5%** |
| 金控 | 集團資本適足率 | **≥ 100%** |
| 保險 | RBC（美）／SMR（日）／SCR（歐） | **≥ 200%** |
| REIT | LTV（貸款價值比） | **< 50.0%** |

🔴 一檔只能套一種指標，而且必須與評分 §6.5 用的公式一致。
🔴 **產業別只能填這五個值**：`一般`、`銀行`、`金控`、`保險`、`REIT`。不可以寫「一般企業」「保險業」等變體。
🔴 總覽「安全指標值」欄填的指標，必須與該列產業別對得上（保險填 RBC、金控填集團適足率，不可以填負債比）。

### 5.2 資料時效

| 資料類型 | 可接受時效 | 逾期怎麼辦 |
|:---|:---|:---|
| 股價／PE | ≤ 5 個交易日 | 逾期 → 重新取價 |
| 財報數據 | 最新已公布年報／季報；TTM 含最近四季 | 只有更舊的 → 標 `管理指標偏舊`，記進「四、待查」 |
| 市佔資料 | ≤ 24 個月 | 24~36 個月可用但要註明日期；**> 36 個月 → `資料缺漏`**，市場地位計 0 分 |

> 所有數據都要標**來源 ＋ 日期**。REIT 另外要附股價淨值比、租金報酬率、空租率。

---

## 6. 五維度評分（滿分 100）

### 6.0 通用規則

```
clamp(min, max, x) = max(min, min(max, x))

計算順序：子項分（小數 2 位）→ 維度分（四捨五入到小數 1 位）→ 總分 = 五個維度直接相加
```

| 維度 | 配分 |
|:---|:-:|
| 一、市場地位 | 25 |
| 二、成長性 | 25 |
| 三、獲利品質 | 20 |
| 四、估值 | 20 |
| 五、財務安全 | 10 |
| **合計** | **100** |

### 6.1 市場地位（25 分）

```
子項1  名次基準分（三選一）：第 1 名 = 18.0 ｜ 第 2 名 = 14.0 ｜ 第 3 名 = 10.0
子項2  市佔率加成：min(4.0, 市佔率% × 0.1)
子項3  護城河加分：每具備一項 +1.0，上限 3.0 → 只能是 0/1/2/3 的整數
                  （高轉換成本 / 網路效應 / 規模經濟 / 特許獨佔）
維度分 = clamp(0, 25.0, 子項1 + 子項2 + 子項3)
```

🔴 計分前提：
- 名次與市佔率必須來自 ⑤-1 的具名資料。只寫「龍頭」「領先」而沒有具名數據 → 子項 1、2 都計 0 分。
- 護城河每加 1 分都要在 ⑤-3 附量化證據，**不可以給 0.5 分**。

### 6.2 成長性（25 分）

```
子項1  5 年淨利 CAGR：  clamp(0, 15.0, 5年CAGR% × 0.75)
子項2  10 年淨利 CAGR： clamp(0, 10.0, 10年CAGR% × 0.50)
維度分 = 子項1 + 子項2
```

> 沒有 10 年數據，但有 ≥ 7 年可以替代（要標註）；< 7 年 → 子項 2 計 0 分。

### 6.3 獲利品質（20 分）

```
OPM% = (營業利益率 ÷ 稅前淨利率) × 100%

子項1  OPM 分數：        max(0, 8.0 − |OPM% − 100| × 0.1)
子項2  營業利益率分數：  min(6.0, 營業利益率% × 0.20)
子項3  5 年 ROE 中位數： clamp(0, 6.0, 5年ROE中位數% × 0.30)
維度分 = 子項1 + 子項2 + 子項3
```

> OPM 愈接近 100% 分數愈高，代表營業外損益的干擾愈小。

### 6.4 估值（20 分）

```
子項1  PE 分數：           clamp(0, 12.0, 12.0 × (15.0 − PE) ÷ 10.0)
子項2  5 年殖利率中位數：  min(8.0, 5年殖利率中位數% × 1.60)
維度分 = 子項1 + 子項2
```

> G2 規定 PE 嚴格落在 (5, 15) 開區間，所以子項 1 的合法值一定在 (0, 12.0)。算出 ≥ 12.0 代表 PE ≤ 5.0，這檔應該被 G2 擋掉 → 跑程序 A。

### 6.5 財務安全（10 分，依產業五選一）

```
一般：   clamp(0, 10.0, 10.0 × (70.0 − 負債比%) ÷ 70.0)
銀行：   clamp(0, 10.0, (CAR% − 10.5) × 2.0)
金控：   clamp(0, 10.0, (集團適足率% − 100) ÷ 5.0)
保險：   clamp(0, 10.0, (RBC% − 200) ÷ 15.0)
REIT：   clamp(0, 10.0, 10.0 × (50.0 − LTV%) ÷ 50.0)
```

### 6.6 完整計算範例（照這個格式算）

假設某標的：市佔第 1 名、市佔率 57.0%、護城河 2 項；5 年淨利 CAGR 18.0%、10 年 12.0%；營業利益率 52.5%、稅前淨利率 55.0%、5 年 ROE 中位數 21.0%；PE 6.73、5 年殖利率中位數 4.5%；一般企業、負債比 27.09%。

```
市場地位 = 18.0 + min(4.0, 57.0×0.1=5.70) + 2 = 18.0 + 4.00 + 2 = 24.00 → 24.0
成長性   = clamp(0,15, 18.0×0.75=13.50) + clamp(0,10, 12.0×0.50=6.00) = 19.50 → 19.5
獲利品質 : OPM% = 52.5 ÷ 55.0 × 100 = 95.45
           子項1 = max(0, 8.0 − |95.45−100|×0.1) = 8.0 − 0.46 = 7.54
           子項2 = min(6.0, 52.5×0.20=10.50) = 6.00
           子項3 = clamp(0,6, 21.0×0.30=6.30) = 6.00
           = 7.54 + 6.00 + 6.00 = 19.54 → 19.5
估值     : 子項1 = clamp(0,12, 12.0×(15.0−6.73)÷10.0 = 9.924) = 9.92
           子項2 = min(8.0, 4.5×1.60=7.20) = 7.20
           = 17.12 → 17.1
財務安全 = clamp(0,10, 10.0×(70.0−27.09)÷70.0 = 6.13) = 6.13 → 6.1
總分     = 24.0 + 19.5 + 19.5 + 17.1 + 6.1 = 86.2
```

### 6.7 不變式 I1~I7（評分完與稽核時逐條檢查）

任一條違反 ＝ 算術錯誤，當場重算並記進「二、修正紀錄」。

| # | 不變式 | 抓什麼 |
|:-:|:---|:---|
| **I1** | 市場地位 ＝ `{18/14/10}` ＋ `min(4, 市佔%×0.1)` ＋ k，**k 只能是 0/1/2/3 整數** | 護城河給了 0.5 分 |
| **I2** | 每個維度 ≤ 配分上限（25／25／20／20／10） | 子項加總溢位 |
| **I3** | 估值子項 1 落在 (0, 12.0) 開區間 | ≥ 12 代表 PE ≤ 5，應該被 G2 擋掉 |
| **I4** | 總分 ＝ 五個維度（都已四捨五入到 1 位）直接相加 | ⑦ 的驗算列必須等於五項相加 |
| **I5** | 若 ⑤-1 標 `⚠️ 市佔資料不足` → 市場地位 ≤ 3.0 | 沒具名卻拿 20 幾分 |
| **I6** | 總覽的 PE／營益率／安全指標值三欄都是具體數值 | 抓 `>10%` 或填錯欄位 |
| **I7** | 總覽該列的五維度與總分，和個股 md ⑦ 逐字相同 | 兩處不同步 |

### 6.8 缺失值與特殊情況

| 情況 | 怎麼處理 |
|:---|:---|
| 子項查無公開數據 | 計 **0 分**，標 `資料缺漏`，嚴禁臆測 |
| 稅前淨利率 ≤ 0 | OPM 子項計 0 分，標 `OPM 不適用` |
| 引用財務絕對金額 | 標來源日期，**強制換算每股金額** |
| 財務安全標註 | 要寫明公式類型與代入值，例如 `財務安全 6.2（REIT／LTV 19.0%）` |

### 6.9 同分排序

總分相同時依序比：① PE 較低 → ② 5 年淨利 CAGR 較高 → ③ 財務安全較高 → ④ 代號升冪。

---

## 7. 標準執行流程（步驟 1~11，順序不可跳）

> **本章的步驟編號就是執行順序。** 從步驟 1 做到步驟 11。

| 步驟 | 做什麼 | 詳細規格 |
|:-:|:---|:---|
| 1 | 讀入狀態（規格檔、主結果檔、所有 csv） | §7.1 |
| 2 | 跑程序 B：孤兒列清理 | §4.3 |
| 3 | 跑稽核：重新分析 20 檔榜上股 | §7.3 |
| 4 | 算「說明缺口」，分配本輪額度 | §7.4 |
| 5 | 抽樣＋驗三大門檻（台→美→港→日） | §7.5 |
| 6 | 驗剩餘門檻 G1／G5／G6／G7 | §7.6 |
| 7 | 評分 ＋ 立刻寫個股 md（單檔閉環） | §7.7 |
| 8 | 單市場存檔與體檢，換下一個市場 | §7.8 |
| 9 | 排名整合、重新命名、刪落榜檔 | §7.9 |
| 10 | 跑程序 C：Top 160 同步刪 csv | §4.4 |
| 11 | 收工自查清單 ＋ 存檔 ＋ 回報 | §7.11 |

**本輪最重要的產出**：每檔 Top 160 個股的 `<排名>-<代號>-<公司>.md`，內容含完整 ①~⑦。主結果檔只是索引與總覽。

### 7.1 步驟 1：讀入狀態

1. 讀本規格檔。
2. 讀主結果檔。不存在就用第 9 章骨架新建；**讀取失敗時絕不可以用空骨架覆蓋**。
3. 讀「一、執行摘要」的 Checkpoint 與「五、下一輪待辦」，確定本輪稽核區間是第幾輪。
4. 列出並讀取所有 `.csv`，依 §7.2 建立四個市場的候選池，記下各市場剩餘檔數。
   - 一個 csv 都找不到 → 本輪不抽新股，只做稽核與補說明。
5. **補做上輪沒完成的刪列** 🔴：「五、下一輪待辦」的「待刪 csv 列」不是空的 → 立刻依 §4.2／§4.4 補刪並驗證，再往下走。
6. **開場一致性檢查**：抽查現行 Top 160 代號有沒有誤留在 csv（上輪程序 C 是否漏做）→ 有殘留就當場補做程序 C，記進「二、修正紀錄」。

### 7.2 抽樣母體：csv 候選池

**讀取範圍**：每輪開場列出並讀完 `StkScreenerResult\` 底下**所有** `.csv`，全部併起來當本輪候選池。沒有別的抽樣來源。

🔴 **候選池語意**：csv 裡留著的都是**還沒入榜**的標的。
- csv 與 Top 160 **互斥**，正常情況抽不到榜上標的。
- 如果真的抽到已經在 Top 160 的代號 → 代表上輪程序 C 有遺漏 → **直接刪列（補做程序 C）、不佔額度、回頭重抽**，並記進「二、修正紀錄」。這檔的維護交給稽核，抽樣階段不處理。
- 曾入榜又落榜的標的**不會回到 csv**，本輪不會再抽到，這是預期行為。

**市場歸屬判定（依序判斷，第一個命中就採用）**：

| 順序 | 依據 | 規則 |
|:-:|:---|:---|
| 1 | **檔名**（優先） | `TW`/`台` → 台股；`US`/`美` → 美股；`HK`/`hk`/`港` → 港股；`JP`/`jp`/`日` → 日股 |
| 2 | csv 市場欄位 | 有 `market`／`exchange`／`市場`／`交易所` 欄位就採用 |
| 3 | 代號格式 | 5 位含前導 0 → 港股；純英文 → 美股；4 位純數字 → 台／日分不出來，標 `市場不明` |

> 台股與日股同樣是 4 位數字（`2330` vs `9101`），代號格式分不出來，所以檔名優先。
> 判不出來 → 標 `市場不明`，本輪跳過不抽，記進「四、待查」，**不刪 csv**。

**欄位要求**：至少要能取得「股票代號」。公司名、市場欄位有就用，沒有就自己查補。**不要因為欄位名對不上就重寫 csv 結構。**

**抽樣優先序（只影響「先抽誰」，不影響「過不過」）**：
- csv 已含 `市盈率(TTM)`／`PER`、`經營利潤率`、`股息率TTM`、`ROE(%)`、`負債總額佔比(%)` 等欄位。
- 優先抽 csv 上 PE 落在 6~14、營益率 > 12%、ROE > 12% 的列。
- csv 上明顯出界的列，仍然要用即時數據確認後才可以判定不合格。
- 🔴 門檻判定一律以**即時查證的最新數據**為準。

**候選池耗盡**：某市場 csv 全抽完或全被刪 → 停掉該市場，在「一、執行摘要」與「四、待查」寫 `<市場> csv 候選池耗盡（剩餘 X 檔）`，在「五、下一輪待辦」請使用者補充 csv。**不得用外部標的遞補。**

### 7.3 步驟 3：稽核（SOP-AUDIT，每輪 20 檔，不佔額度）

**輪次對照表**（本輪是第幾輪看「五、下一輪待辦」）：

| 輪次 | 稽核區間 |
|:-:|:---|
| 1 | TOP 1~20 |
| 2 | TOP 21~40 |
| 3 | TOP 41~60 |
| 4 | TOP 61~80 |
| 5 | TOP 81~100 |
| 6 | TOP 101~120 |
| 7 | TOP 121~140 |
| 8 | TOP 141~160 → 下輪回到 1 |

> 🔴 榜單不滿 160 檔時，稽核區間**照名次區間取**，區間內有幾檔就稽核幾檔（例如榜單目前 112 檔、本輪是第 6 輪 → 稽核 TOP 101~112 共 12 檔）。**不可以**挪用其他區間的標的來湊滿 20 檔，也不可以因此跳過這一輪。

🔴 **這一步不是「對帳」，是「重做一次分析」。** 榜上標的已經從 csv 移除，往後**只有這裡**會再碰它們——稽核做得多細，榜單就多新。
🔴 **禁止只比對數字就宣告通過。沒有重新查證最新數據 ＝ 這一步沒做。**

**單檔七個動作（一檔做完才做下一檔）**：

| # | 動作 | 要求 |
|:-:|:---|:---|
| **A1** | **讀回**既有個股 md 的 ①~⑦ 與總覽該列 | 記下目前總分，等一下要比對分數變動 |
| **A2** | **重查最新數據**（不可沿用 md 舊值） | 最新收盤價、近四季 EPS、營益率（年度＋TTM）、ROE（最新＋5 年中位數）、安全指標、殖利率、淨利 CAGR、市佔率。每一項都要有**來源 ＋ 日期** |
| **A3** | **重驗七道門檻 G1~G7** | 標準與第 5 章完全相同，不因為「已經在榜上」而放寬。時效不合（§5.2）→ 重新取數 |
| **A4** | **重算五維度與總分** | 依第 6 章公式從頭算，**不是**沿用舊分數微調 |
| **A5** | **套不變式 I1~I7** | 任一條違反 → 當場重算並記進「二、修正紀錄」 |
| **A6** | **抓錯**：逐條跑 §7.3.1 的 E1~E9 | 命中就當場改檔 |
| **A7** | **補新資料**：把 A2 查到的新事實寫回 ②③④⑤⑥⑦，更新「本區塊最後更新」與「本輪稽核」兩行，同步總覽該列 | 新舊數據不同時以**新的**為準，舊值不用保留 |

**重驗結果分流**：

| 結果 | 怎麼處理 |
|:---|:---|
| 七道全過、分數變動 < 3.0 分 | 更新 md 與總覽，計「稽核維持」 |
| 七道全過、分數變動 ≥ 3.0 分 | 同上，另外在「二、修正紀錄」寫 `<代號> 總分 XX.X → XX.X（主因：<哪個維度><為什麼>）`，計「稽核重評」 |
| **任一道門檻沒過** | 🔴 **踢出榜單**：刪掉該檔個股 md、從總覽移除、其餘名次遞補重排，在「二、修正紀錄」寫明卡在哪一道與實際數值，計「稽核踢出」 |
| 關鍵數據查不到／無法驗證 | 視同沒過 → 踢出，並記進「四、待查」，寫明查過哪些來源 |

> 被踢出的標的**不回填 csv**，下輪不會再抽到。空缺由當輪或後續輪次的新抽樣自然補上，**嚴禁**為了湊滿 160 檔而放寬門檻。

**存檔節奏**：每稽核完 **5 檔**存檔一次；20 檔全做完再整體存檔一次，然後才進步驟 4。

#### 7.3.1 稽核錯誤清單 E1~E9（A6 逐條套）

| # | 錯誤類型 | 怎麼抓 | 怎麼修 |
|:-:|:---|:---|:---|
| **E1** | **算術錯** | ⑦ 子項相加 ≠ 維度分；五維度相加 ≠ 標示的總分 | 以第 6 章公式重算 |
| **E2** | **兩處不同步** | 總覽該列的五維度／總分／PE／營益率／安全指標值 ≠ 個股 md ⑦⑤ | 用重算後的新值同時覆蓋兩處 |
| **E3** | **不變式違反** | I1~I7 任一條 | 重算並記錄 |
| **E4** | **模板缺漏** | ①~⑦ 缺區塊；⑤-1／⑤-2／⑤-3 缺表；① 不足 3 點；②③ 不足 3 點；④ 不足 2 條 | 補齊，缺資料就去查 |
| **E5** | **數值搪塞** | 出現 `>10%`、`約`、`逾`、空白、`N/A` 卻還是給了分 | 補實際數值；查無 → 該子項 0 分並標 `資料缺漏` |
| **E6** | **資料過期** | 股價／PE > 5 個交易日；市佔 > 24 個月；財報不是最新已公布 | 重新取數；市佔 > 36 個月 → 市場地位計 0 分 |
| **E7** | **禁用詞殘留** | §8.2 禁用詞單獨出現、後面沒接具名數據 | 當場改寫成「產品＋市場＋名次＋市佔率＋來源」 |
| **E8** | **檔名／連結錯** | 排名前綴補零錯、檔名與總覽連結不符、連結指向不存在的檔 | 依 §1.2 更名並修連結 |
| **E9** | **每股化缺漏** | 提到營收／營業利益／自由現金流等絕對金額，卻沒換算每股金額 | 補上 `（換算約每股 X.XX <幣別>）` |

> 每命中一項就記一筆「二、修正紀錄」，並在「一、執行摘要」累計「稽核修正 X 處」。
> E1~E3 是**算術與一致性**，E4~E9 是**內容品質**，兩類都要跑，不可以只跑一類。

### 7.4 步驟 4：算說明缺口，分配額度

```
說明缺口 = 總覽筆數 − 內容完整（①②③ 齊全 且 ⑤ 市佔已具名）的個股 md 檔數
```

| 缺口 | 額度怎麼分配 |
|:---|:---|
| **> 額度的 1/2**（> 10） | 全部拿去補個股 md，不抽新股，補完直接跳步驟 9 |
| **1 ~ 額度的 1/2**（1~10） | 先補滿缺口，剩下的才抽新股 |
| **= 0** | 全部抽新股（每個市場各抽滿每市場配額） |

> 榜單不滿 160 檔的擴充期，缺口 = 0 時一律走「全部抽新股」。

### 7.5 步驟 5：抽樣主迴圈

**市場順序固定：台股 → 美股 → 港股 → 日股。一個市場完整跑完才換下一個。**

```
FOR 市場 IN [台股, 美股, 港股, 日股]:
    正式樣本 = 0
    WHILE 正式樣本 < 每市場配額(5):

        A. 候選池已空 → 記「候選池耗盡」→ 換下一個市場

        B. 從 csv 抽 1 檔（依抽樣優先序），記住它來自哪個 csv
           · 避開本輪已經抽過的
           · 如果已在 Top 160 → 走 D（上輪程序 C 漏刪）

        C. 立刻查最新收盤價、EPS、營益率、ROE（不沿用 csv 值）
           算 TTM PE = 最新收盤價 ÷ 近四季 EPS
           驗三大門檻 G2/G3/G4：
             ├ 沒過 → 跑程序 A → 回 A（不佔額度）
             └ 通過 → 進步驟 6 驗 G1/G5/G6/G7
                        ├ 沒過 → 跑程序 A → 回 A
                        └ 全過 → 進步驟 7 → 正式樣本 += 1

        D. 【榜上標的誤留 csv】（屬異常）：
           1) 補做程序 C：把該列從 csv 刪掉並驗證
           2) 記一筆「二、修正紀錄」：<代號> 應於上輪同步刪列
           3) 不佔額度、不重評（這檔交給稽核處理）→ 回 A 重抽

        E. 每累積 5 檔正式樣本 → 立刻存檔（避免中斷時整批遺失）
```

**一律拒絕並跑程序 A 的情況**：門檻任一項沒達標、EPS ≤ 0、數據缺漏／過期、口徑不明、來源衝突。
**不可硬湊**：候選池抽乾湊不滿配額時，不可以用邊界值、過期值或外部標的遞補。

**重抽軌跡記錄**：`市場｜來源csv｜代號｜被哪道擋下｜資料日期｜拒絕原因｜是否已從csv移除`。
結果檔只在「一、執行摘要」寫重抽次數與移除筆數，不要污染 Top 160 表格。

### 7.6 步驟 6：產業判定與剩餘門檻

1. 判定產業別：`一般`／`銀行`／`金控`／`保險`／`REIT`（決定 G5 用哪個指標）。
2. 逐條查驗 G1、G5、G6、G7。

| 結果 | 怎麼處理 |
|:---|:---|
| 七道全過 | 進步驟 7 |
| 任一道沒過 | 跑程序 A → 回步驟 5 重抽 |
| 數據不足 | 視為沒過，同上 |

### 7.7 步驟 7：評分與個股撰寫（單檔閉環）

七道全過之後，**立刻做完這四件事，才可以處理下一檔**：

1. 依第 6 章公式算五維度分數與總分。
2. 套不變式 I1~I7 自檢，過不了就重算。
3. 依第 8 章模板寫完個股 md 的 ①~⑦。
4. 更新主結果檔「三、Top 160 排名總覽」的那一列。

- ✅ 正確：`A 評分 → 寫 A.md → B 評分 → 寫 B.md`
- ❌ 錯誤：`A 評分 → B 評分 → C 評分 → 最後才補寫 md`

### 7.8 步驟 8：單市場存檔與市場推進

該市場配額跑完或候選池耗盡後存檔，並體檢：

- [ ] 主結果檔 `## ` 大標題剛好 5 個（一~五）
- [ ] 子項分與總分算術無誤，通過不變式
- [ ] 總覽表格與個股 md 數據一致、排名不重複
- [ ] 本市場被拒絕的標的**每一檔**都跑完了程序 A

額度還沒用完而且還有市場沒跑 → 回步驟 5 換下一個市場；額度用完 → 進步驟 9。

### 7.9 步驟 9：排名整合與落榜清理

1. **去重**：同一家公司多地上市時，只保留主要上市地的原股。
2. **重排序與重新命名**：依總分由高到低排（同分依 §6.9 判定），更新名次，改個股 md 檔名前綴。
3. **落榜檔強制刪除** 🔴：目錄下凡是不在 Top 160 的個股 `.md` 一律刪除。`0.StkScreenerResult_gemini.md` 與所有 `.csv` **不可以刪**（csv 只能刪「列」不能刪「檔」）。
   - 🔴 **刪檔一定要同時刪列**：刪掉個股 md 的同時，一定要把總覽那一列也移除。只刪檔不刪列 → 下輪開場會被程序 B 當孤兒列清掉，等於把問題往後推一輪。
4. **完成度指標**（填進「一、執行摘要」）：
   - **A** ＝ 總覽排名筆數
   - **B** ＝ 目錄下 Top 160 個股 md 檔數
   - **C** ＝ ①②③ 齊全的檔數
   - **D** ＝ ⑤ 市佔已具名合格的檔數
   - 🔴 **合格條件：`A = B` 且 `C = B` 且 `D = B`。**
   - 若 `B < A` → 代表本輪又產生了孤兒列 → **當場補做程序 B**，不可以留到下輪。
   - 若 `C < B` 或 `D < B` → 當場補齊內容。
5. **三大門檻全樣本稽核**：逐檔複查正式樣本的 G2／G3／G4（包含 `>10%` 這種沒有實際數值的寫法），不合格的跑程序 A 重抽。
6. **市佔敘述稽核**：掃描 §8.2 禁用詞，命中處當場改寫並記進「二、修正紀錄」。
7. **標註下輪稽核區間**：在「五、下一輪待辦」註明（本輪第 N 輪 → 下輪 N+1，第 8 輪之後回到 1）。

### 7.10 步驟 10：跑程序 C

依 §4.4 執行。必須在步驟 9 第 2 點「重排序與重新命名」之後做，確保用的是最終名單。

跑完再做一次 csv 收尾稽核：
- 比對「被擋下的標的」與「已從 csv 移除的代號」→ 必須完全一致，有落差當場補做程序 A。
- 逐一抽查 Top 160 代號在對應 csv 確實查無，有殘留當場補做程序 C。
- 重讀四個 csv，確認 BOM／表頭／格式與改動前一致。
- 把移除筆數（淘汰 ＋ 同步）與剩餘候選數寫進「一、執行摘要」。

### 7.11 步驟 11：收工自查、存檔與回報

**收工自查清單（逐條打勾）**

*結構與檔名*
- [ ] 主結果檔 `## ` 大標題剛好 5 個（一~五）
- [ ] 沒有 `TOP01-...` 這種舊檔名殘留
- [ ] 目錄下沒有不在 Top 160 的個股檔
- [ ] 總覽表格的排名、總分、公司名與個股 md 完全一致
- [ ] 總覽公司欄都是 `[公司](<檔名.md>)` 格式，而且檔案存在
- [ ] 🔴 程序 B 已執行：總覽**每一列**都找得到對應個股 md（孤兒列 0 列）
- [ ] 本輪清掉的孤兒列，代號已同步從對應市場 csv 移除
- [ ] 本輪刪掉的個股 md（落榜／稽核踢出），總覽對應列也都移除了

*抽樣與門檻*
- [ ] 所有正式樣本 PE 嚴格落在 5.0~15.0 之間（不含邊界）
- [ ] 營益率、ROE、安全指標都寫了實際數值，沒有 `>10%` 搪塞
- [ ] 所有標的都來自 csv，沒有外部來源
- [ ] 門檻判定用的是即時查證數據，不是 csv 舊值
- [ ] 被擋下的標的都當場跑完了程序 A
- [ ] 產業別只有 `一般/銀行/金控/保險/REIT` 五種，安全指標與產業別對得上

*稽核*
- [ ] 本輪稽核區間**每一檔**都重新查過最新數據（不是只比對舊值）
- [ ] 每檔都重驗過 G1~G7、重算過五維度總分
- [ ] E1~E9 逐條跑完，命中處已當場修正
- [ ] 每檔 md 的「本區塊最後更新」與「本輪稽核」兩行已更新為今天
- [ ] 稽核踢出的標的：md 已刪、總覽已移除、名次已遞補重排
- [ ] 稽核造成的分數變動已同步到總覽表格

*csv 完整性*
- [ ] 被刪代號重讀 csv 確實查無；沒有另存／合併／新建 csv
- [ ] 被改動 csv 的 BOM／表頭／引號風格／編碼／換行與改動前一致
- [ ] 港股前導 0（`00151`）與日股千分位（`"1,861"`）寫回後沒被破壞
- [ ] 沒有「判定不合格但還留在 csv」的標的
- [ ] 🔴 程序 C 已於**本輪收尾**執行：**Top 160 全部代號在對應 csv 一律查無**
- [ ] 刪列只動對應市場的 csv（台股代號沒去刪到 jp.csv，反之亦然）
- [ ] 四個 csv 都還在（只刪列，沒刪檔、沒清空、沒新增列）

*內容品質*
- [ ] 每檔 ⑦ 五維度相加 ＝ 標示的總分
- [ ] 全數通過不變式 I1~I7
- [ ] 禁用詞掃描 0 處殘留
- [ ] 「五、下一輪待辦」已填（每輪必填，不得留空）

**最終存檔與回報**

1. 寫入 `StkScreenerResult\0.StkScreenerResult_gemini.md`。
2. 再讀一次驗證：日期是當天、區塊沒有變少、`## ` 大標題 5 個、個股檔連結有效。
3. **最後一次 csv 驗證** 🔴：重讀四個 csv，確認 ①四檔都在 ②BOM／表頭／引號風格完好 ③Top 160 代號全部查無。
4. **對話框只輸出精簡摘要（第 10 章格式）**，嚴禁貼完整檔案內容或大表格。

---

## 8. 個股詳細檔撰寫規格

檔案路徑：`StkScreenerResult\<排名>-<代號>-<公司>.md`
必須包含 **① 到 ⑦**（⑤ 含 ⑤-1、⑤-2、⑤-3 三張必填子表）。

### 8.1 個股 md 模板

````markdown
### 第 N 名：<代號> <公司名稱> — 總分 XX.X / 100（<市場>／<產業別>）
- **本區塊最後更新**：YYYY-MM-DD
- **本輪稽核**：YYYY-MM-DD｜G1~G7 重驗 ✅｜總分 XX.X → XX.X｜本輪補充：<新財報/新市佔/新事件，沒有就寫「無新增，數據沿用 YYYY-MM-DD 來源並已複查時效」>
  （新入榜的檔第一次寫入時填 `新入榜（YYYY-MM-DD 首次分析）`，之後每次稽核都要覆寫這行）
- **一句話定位**：<點名「什麼產品/服務」在「什麼市場」排第幾，100~500 字>

**① 為什麼能進 Top 160**（必列 3 點，每點 ＝ 維度 ＋ 分數 ＋ 具名產品市場）
1. <例：市場地位 24.0/25 — 中國海域原油產量第 1、市佔 57.0%>
2. <例：成長性 21.4/25，5 年淨利 CAGR 達 22.5%>
3. <例：獲利品質 19.2/20，營益率 38.5% 且 OPM 95.8%>

**② 優點（買它的理由）**（3~5 點，每點 ＝ 事實 ＋ 數據 ＋ 來源日期）
| # | 優點 | 具體數字 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**③ 缺點與風險（不買它的理由）**（3~5 點，嚴禁寫「無明顯風險」）
| # | 缺點 / 風險 | 具體數字或事件 | 來源 / 日期 |
|:-:|:---|:---|:---|
| 1 | ... | ... | ... |

**④ 什麼情況會被踢出榜單**（2~3 條量化觸發條件）
- <例：TTM PE 升破 15.0（目前 X.X，尚有 XX% 緩衝）>

**⑤ 硬性門檻查驗**（🔴 五項都要寫實際數值，禁止 `>10%`）：
PE X.XX ✅｜營業利益率 XX.X%（年度 XX.X% / TTM XX.X%）✅｜ROE XX.X%（5 年中位數 XX.X%）✅｜<安全指標> XX.X% ✅｜非 ADR 普通股・非 OTC（<交易所>）✅

**⑤-1 市佔定位（🔴 六欄缺一即 `資料缺漏`）**
| 主力產品/服務 | 市場範圍 | 排名 | 市佔率 | 統計口徑 | 來源/日期 |
|:---|:---|:-:|:-:|:---|:---|
| <具體產品名，不可寫「本業」> | <全球/區域/國家> | 第X名 | XX.X% | <營收/出貨量等> | <機構, YYYY-MM-DD> |

**⑤-2 競爭格局（🔴 必填）**
| 排名 | 公司 | 市佔率 | 與本公司差距 |
|:-:|:---|:-:|:---|
| 1 | ... | XX.X% | ... |

**⑤-3 為什麼能排在這個名次**（2~4 點 ＝ 可驗證機制 ＋ 量化證據，不可只寫「品牌強」）
- <例：成本優勢 — 桶油成本 28.5 美元，低於 Shell 34.2、PetroChina 41.0（各公司 2025 年報）>

**⑥ 財務數據**：最新 EPS <金額/每股>｜預估 EPS｜5 年 ROE 中位數 XX.X%｜5 年殖利率中位數 XX.X%｜OPM XX.X%｜5 年淨利 CAGR XX.X%｜10 年淨利 CAGR XX.X%
（每一項都要附來源日期；REIT 另附股價淨值比、租金報酬率、空租率）

**⑦ 分數拆解**：市場地位 XX.X（名次 XX.XX + 市佔 XX.XX + 護城河 X）｜成長性 XX.X（5年 XX.XX + 10年 XX.XX）｜獲利品質 XX.X（OPM XX.XX + 營益率 XX.XX + ROE XX.XX）｜估值 XX.X（PE XX.XX + 殖利率 XX.XX）｜財務安全 XX.X（<產業別>／<指標值>）
- **總分驗算**：XX.X + XX.X + XX.X + XX.X + XX.X = **XX.X**
- **不變式自檢**：I1 ✅｜I2 ✅｜I3 ✅｜I4 ✅｜I5 ✅｜I6 ✅｜I7 ✅
````

### 8.2 市佔敘述具名規範

**核心要求**：提到公司地位，必須回答六個問題——**哪個產品**、**哪個市場**、**第幾名**、**市佔多少 %**、**贏過誰**、**憑什麼**。只寫「龍頭」而沒回答 → 視為未完成。

**禁用詞（單獨出現就算不合格）**：`龍頭`、`領先`、`領頭`、`卓越`、`居前列`、`名列前茅`、`優秀標的`、`行業代表`、`代表標的`、`巨擘`、`具備高度行業競爭優勢`、`市佔率居區域前列`、`市場獨佔或領頭地位`。

> 例外：上述詞如果**緊接**具體「產品 ＋ 市場 ＋ 名次 ＋ 市佔率 ＋ 來源」，可以保留當修飾語。

**改寫範例**：

| ❌ 不合格 | ✅ 合格 |
|:---|:---|
| 港股該行業龍頭代表標的，市場地位卓越。 | 中國海域原油產量排名第 1、市佔 57.0%（2025 年產量口徑，公司年報 2026-03-26）；第 2 名中石化 21.0%、第 3 名中石油 18.0%。 |

**多角化公司**：就營收占比最高的前 2 項業務，分別列出名次與市佔率。
**查無第三方市佔**：⑤-1 標 `⚠️ 市佔資料不足`，子項 1、2 計 0 分，記進「四、待查」。

---

## 9. 主結果檔標準骨架

`StkScreenerResult\0.StkScreenerResult_gemini.md` **必須且只能包含以下 5 個 `## ` 大標題**（一~五），不可增減。個股詳細分析一律寫進獨立的 `<排名>-<代號>-<公司>.md`，主結果檔在總覽表格的公司欄直接用 Markdown 連結指過去。

🔴 **總覽表格填寫規則**：
- 「市佔定位」格式固定為 `<產品>｜<市場>｜第 X 名｜XX.X%`，四段缺一不可。
- 「產品」欄 **≤ 30 字、語意必須完整**，🔴 嚴禁被截斷（不可出現沒收尾的括號、引號或半句話）。詳細描述寫在個股 md，不要塞進總覽。
- 「PE」欄填 `X.XX`（2 位小數）。
- 「營益率」欄填 `XX.X%`，**至少 1 位小數**，禁止 `>10%`。
- 「安全指標值」欄填 `<指標名> XX.X%`（如 `負債比 38.2%`、`CAR 14.5%`、`RBC 300.0%`），指標名必須與該列產業別一致。
- 「產業別」欄只能是 `一般`／`銀行`／`金控`／`保險`／`REIT`。

````markdown
# 全市場選股篩選結果（Gemini）
> 更新日期：YYYY-MM-DD_hh:mm:ss
> 篩選條件：市佔前3 ∪ 產品市佔前3、嚴格 5<PE<15（不含邊界）、營業利益率>10%、ROE>10%、財務安全（一般 負債比<70%／銀行 CAR≥10.5%／金控 集團適足率≥100%／保險 RBC≥200%／REIT LTV<50%）、排除普通股 ADR 與 OTC（ADR 特別股不排除）

## 一、執行摘要
- **抽樣母體（csv）**：讀入 X 個 csv｜台 X／美 X／港 X／日 X｜市場不明 X
- **本次抽樣**：台 X／美 X／港 X／日 X（合計 X / 20；只計正式樣本）｜新入榜 X
- **🔴 孤兒列清理（程序 B）**：本輪清除 X 列（代號：…）｜同步 csv 刪列 X 檔（台 X／美 X／港 X／日 X）｜清理後總覽 X 列
- **csv 淘汰刪列（程序 A）**：本輪移除 X 檔（台 X／美 X／港 X／日 X）
- **🔴 csv 榜單同步刪列（程序 C）**：本輪同步移除 X 檔（台 X／美 X／港 X／日 X）｜Top 160 代號殘留 `✅ 0 檔`｜刪列後剩餘 台 X／美 X／港 X／日 X
- **初篩重抽紀錄**：台 X／美 X／港 X／日 X 次
- **Checkpoint 完成度**：`✅台 ✅美 ⬜港 ⬜日`（第 N/8 輪）
- **本次額度使用**：補說明 Z｜初篩 X｜門檻通過 Y｜深度分析 Y（合計 ≤ 20）
- **結果**：新入榜 X｜落榜 X｜修正 X 處
- **Top 160 門檻分數**：XX.X 分
- **榜單**：X / 160 檔
- **說明完成度**：A=XX　B=XX　C=XX　D=XX（合格條件 A=B=C=D）
- **市佔敘述改寫**：本輪改寫 X 處（禁用詞：`✅ 0 處`）
- **🔴 稽核重新分析**：完成 TOP XX~XX（第 N/8 輪）共 X 檔全部重查重評｜稽核維持 X｜稽核重評 X（分數變動 ≥3.0）｜稽核踢出 X｜補充新資料 X 檔｜稽核修正 X 處（E1~E9）｜不變式違反 X 處

## 二、修正紀錄 (Self-Fix Log)（只保留最新 5 筆）
| # | 標的 | 錯誤內容 | 修正後 | 依據來源 | 修正日期 |
|:-:|:--|:--|:--|:--|:--|

## 三、Top 160 排名總覽
> 「市佔定位」格式：`<產品>｜<市場>｜第 X 名｜XX.X%`，產品欄 ≤ 30 字且不可截斷，嚴禁「龍頭」「居前列」。
> 🔴 「公司」欄直接連結對應個股檔（如 `[中國海洋石油](01-0883-中國海洋石油.md)`）。
> 🔴 最後三欄填具體數值：PE 填 X.XX；營益率填 XX.X%（禁 `>10%`）；安全指標值填 `<指標名> XX.X%`。

| 排名 | 代號 | 公司 | 市場 | 產業別 | 市佔定位（產品｜市場｜名次｜市佔率） | 市場地位 (25) | 成長性 (25) | 獲利品質 (20) | 估值 (20) | 財務安全 (10) | 總分 | PE | 營益率 | 安全指標值 |
|:-:|:--|:--|:-:|:-:|:--|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:--|
| 1 | 0883 | [中國海洋石油](01-0883-中國海洋石油.md) | 港股 | 一般 | 海域原油與天然氣開採｜中國海域｜第 1 名｜57.0% | 24.0 | 23.5 | 17.8 | 17.9 | 5.4 | **88.6** | 6.73 | 52.5% | 負債比 27.1% |

## 四、⚠️ 待查 / 資料不足清單
| 代號 | 公司 | 市場 | 卡關項目 | 已試過的來源 | 推測原因 |
|:--|:--|:-:|:--|:--|:--|

（本輪沒有待查標的時，把表格換成一行：`> 本輪無待查／資料不足標的。`）

## 五、下一輪待辦（🔴 每輪必填）
- **待補個股說明**（最優先）：（代號 ＋ 名次）
- **下一輪稽核重分析區間**：（如 TOP 21~40，第 N/8 輪）
- **待改寫的空泛市佔敘述**：（代號 ＋ 名次 ＋ 目前寫法）
- **待修正的不變式違反**：（代號 ＋ 違反哪一條）
- **csv 候選池狀況**：剩餘 台X／美X／港X／日X（都是未入榜標的）
- **待刪 csv 列（上輪寫入失敗者）**：（csv 檔名 ＋ 代號 ＋ 屬程序 A／B／C）
- **下一輪要做的工作**：…
- **未跑完的市場**：…
````

---

## 10. 對話框回覆格式

每輪執行完，對話框**只能**回下面這份精簡摘要，**嚴禁貼完整 Markdown 或大表格**。

```text
✅ StkScreener(gemini) — YYYY-MM-DD_hh:mm:ss
- 完成市場：台/美/港/日（Checkpoint ✅✅✅✅）
- 孤兒列清理（程序 B）：總覽 X 列查無個股 md → 已刪列 ＋ csv 同步刪 X 檔 ✅（清理後總覽 X 列）
- 稽核重分析：TOP XX~XX（第 N/8 輪）X 檔全部重查重評 ✅
  └ 維持 X｜重評 X（分數變動 ≥3.0）｜踢出 X｜補新資料 X 檔｜E1~E9 修正 X 處
- csv 候選池：讀入 X 個 csv｜淘汰刪列 X 檔｜榜單同步刪列 X 檔｜剩餘 台X/美X/港X/日X
- csv 刪列稽核：被擋下 X 檔 = 已刪列 X 檔 ✅｜Top 160 代號殘留 0 檔 ✅（BOM/格式驗證通過）
- 額度使用：補說明 X + 新分析 Y（合計 Z / 20）
- 初篩稽核：正式樣本 Z / 20 皆通過七道門檻 ✅｜重抽 X 次
- 結果：新入榜 X｜落榜 X｜修正 X 處
- 不變式：I1~I7 違反 0 處 ✅（本輪修正 X 處）
- 榜單：X / 160（門檻分數 XX.X）
- 說明完成度：A=XX　B=XX　C=XX　D=XX
- 市佔具名：禁用詞殘留 0 處 ✅｜改寫 X 處
- 下一輪待辦：…
```
