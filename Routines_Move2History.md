# Routines — 公司資料夾雙向歸檔同步 (Two-Way Folder Sync with History)

> **目標狀態（Invariant）**：根目錄的公司資料夾集合 **完全等於** `Routines_CollectsentimentAndReports.md`「每日輪替表」的資料夾集合；不在輪值表的公司一律待在 `History/`。
>
> 每次執行都把實際狀態校正回這個目標狀態，**雙向搬移、永不刪除資料**。

---

## 全域參數 (Global Parameters)

- `ROTATION_FILE`：`Routines_CollectsentimentAndReports.md`（輪值表，**唯一真實依據 / Single Source of Truth**）
- `HISTORY_DIR`：`History`
- `EXCLUDE_DIRS`（非公司資料夾，兩個方向都不參與搬移）：
  - `History`、`AnalysisResult`、`StkScreenerResult`、`Log`、`Prompt`、`gemini`
  - 任何以 `.` 開頭的隱藏資料夾（`.git`、`.claude`、`.github` …）
- `TODAY`：執行當日日期，格式 `YYYY-MM-DD`（用於合併註記）

---

## 集合定義 (Set Definitions)

| 符號 | 定義 |
| :--- | :--- |
| `T` | 輪值表資料夾集合 — `ROTATION_FILE`「每日輪替表」第 4 欄 `本地資料夾名稱 (Folder)` 的所有值 |
| `R` | 根目錄公司資料夾集合 — 根目錄第一層資料夾，扣除 `EXCLUDE_DIRS` 與隱藏資料夾 |
| `H` | 歸檔資料夾集合 — `History/` 第一層資料夾 |

解析 `T` 的注意事項：
- 去除反引號與前後空白；忽略表頭列、`|:---:|` 分隔列、以及值為 `—` 的 Skip 列。
- 同一資料夾可能出現在多列（例如 `3445RS` 同時對應日股 `3445` 與中股 `688432`）→ 去重後視為同一項。

---

## 執行流程 (Execution Flow)

### Step 0 — 前置檢查
- `git status` 必須乾淨（無未提交變更）。若不乾淨 → **中止**，回報請先處理工作區。
- 讀取 `ROTATION_FILE` 建立 `T`；列出根目錄與 `History/` 建立 `R`、`H`。

### Step 1 — 計算兩個方向的工作清單

- **方向 A（下架 / Archive）**：`A = R - T`
  → 根目錄有、但不在輪值表的公司，搬到 `History/`。
- **方向 B（復役 / Restore）**：`B = H ∩ T`
  → 已歸檔、但輪值表又收錄的公司，搬回根目錄。
  - **B1（單純搬回）**：`B - R` — 根目錄沒有同名資料夾 → 直接 `git mv` 搬回。
  - **B2（同名合併）**：`B ∩ R` — 根目錄已有同名資料夾 → 進入 **Step 3 合併程序**。

若 `A`、`B` 皆為空 → 已達目標狀態，**不做任何事**，回報「無需搬移」後結束。

### Step 2 — 執行搬移

一律使用 `git mv` 以保留 git 歷史；**先做方向 A，再做方向 B**（避免中途出現同名衝突）。

- **方向 A**：`git mv "<FOLDER>" "History/<FOLDER>"`
  - 若 `History/<FOLDER>` 已存在 → 這是「根目錄與 History 同時有同名資料夾」的異常狀態，**改走 Step 3 合併程序**（合併到 `History/<FOLDER>`），不得覆蓋。
- **方向 B1**：`git mv "History/<FOLDER>" "<FOLDER>"`
- **方向 B2**：先執行 Step 3 合併，合併完成並驗證後才移除 `History/<FOLDER>` 的空殼。

### Step 3 — 同名資料夾合併程序 (Merge Procedure)

> **最高原則：資訊不滅。** 合併只會讓檔案變多或變長，**絕不能讓任何一邊的內容消失**。

對兩邊同名資料夾內的檔案，逐檔案比對處理：

1. **只有一邊有的檔案** → 直接搬入目標資料夾（`git mv`），內容不動。
2. **兩邊都有、內容完全相同**（`sha256` 或 `git hash-object` 一致）→ 保留目標資料夾那份，另一份直接移除（因為是位元級重複，無資訊損失）。
3. **兩邊都有、內容不同** → **合併成同一個檔案**，規則如下：
   - **主體（Base）**：取「內容較新」的一份為主體。判定順序：
     1. 檔內若有明確報告日期／季度／`最後更新` 標記 → 取較新者；
     2. 否則取 `git log -1 --format=%cI -- <path>` 較新者；
     3. 仍相同 → 取**檔案較大（資訊較多）**者。
   - **附加（Append）**：另一份完整附加在主體末尾，以固定分隔區塊標示來源與時間：

     ```markdown
     ---

     <!-- merged-from: History/<FOLDER>/<FILE> | merged-at: <TODAY> -->
     ## 【合併自歸檔版本】<FILE>

     <另一份的完整原文>
     ```
   - **不做智慧改寫**：不摘要、不刪句、不重排原文，只做「主體 + 分隔線 + 附加」的機械式串接。內容去重交由後續 `ArrangePublicOpinionMd` 等 skill 處理。

4. **依檔案類型的補充判定**（僅影響「誰是主體」，不影響「資訊不滅」）：

   | 檔案樣式 | 主體判定 |
   | :--- | :--- |
   | `*_AnnualReport_<YYYY>.md` | 年份較大者；同年份取檔案較大者 |
   | `*_Quarter_<YYYY>Q<N>.md` | 年份／季度較新者；相同取檔案較大者 |
   | `<YYYY>_PublicOpinion.md` | 年份較大者；同年份取檔案較大者（條目去重不在此處理） |
   | `hourAnalysisResult.md` / 其他分析檔 | git commit 時間較新者 |

5. 合併完成後，`History/<FOLDER>` 應為空 → 移除該空資料夾（git 不追蹤空目錄，`git mv` 完即自然消失）。

### Step 4 — 驗證 (Verification)

搬移／合併後**必須**逐項確認，任一項失敗即回報並停止：

- `R` 重新計算後應**完全等於** `T`（`R - T` 與 `T - R` 皆為空）。
- 每個被搬移的資料夾，搬移前後**檔案數一致**；合併的資料夾，檔案數應為「兩邊聯集」數量。
- 合併過的檔案，其**位元組數 ≥ 兩份原檔中較大者**（確認附加區塊真的寫進去了）。
- `git status` 只應出現 `R`（renamed）與 `M`（modified，合併檔）；**不得出現孤立的 `D`（deleted 而無對應 added）**。
- `git diff --stat` 的刪除行數應僅來自檔案搬移，不得有內容淨損失。

### Step 5 — 反向檢查與回報 (Cross-check & Report)

- **輪值表有、兩邊都找不到**：`T - R - H` → **只回報、不自動建立空資料夾**。可能原因：新加入輪值表尚未收集、資料夾名稱與輪值表拼寫不一致。
- **孤兒歸檔**：`H - T`（留在 `History/` 且不在輪值表）→ 正常狀態，僅列出數量，不動作。
- Commit 並回報，訊息範例：`chore: sync company folders with rotation table (archive N, restore M)`
- 回報內容必須包含：
  1. 方向 A 搬移清單（含檔案數）
  2. 方向 B1 搬回清單（含檔案數）
  3. 方向 B2 合併清單（每個檔案的處理方式：搬入／位元相同去重／合併）
  4. 反向檢查結果（`T - R - H`）
  5. Step 4 驗證是否全數通過

---

## 安全準則 (Safety Rules)

1. **只搬移與合併，永不刪除**：唯一允許的移除是 Step 3-2「位元級完全相同」的重複檔，以及合併完成後的空資料夾殼。其餘任何情況都不得 `rm` 資料夾或檔案。
2. **不覆蓋**：同名一律走合併程序，禁止 `mv -f`、禁止自動改名（如加 `_1`）。
3. **輪值表是唯一依據**：不得以「看起來很舊」「檔案很少」等主觀理由自行決定搬移方向。
4. **只處理第一層**：不遞迴搬移 `History/` 內部的巢狀資料夾，也不拆散公司資料夾內的子目錄結構。
5. **A 先於 B**：先下架再復役，避免中途製造出不必要的同名衝突。
6. **驗證未過不 commit**：Step 4 任一項失敗 → 保留現場、回報，交人工判斷；必要時 `git checkout .` 還原。
7. **合併不改寫語意**：機械式串接，不摘要、不刪節、不翻譯、不重排原文。

---

## 參考：執行紀錄 (Run Log)

### 2026-09-09（第 2 次，雙向版）
- 方向 A（`R - T`）：**無**
- 方向 B（`H ∩ T`）：**無**（`History/` 內 12 個資料夾皆不在輪值表）
- 反向檢查（`T - R - H`）：**無**
- 結論：`R` 已等於 `T`（各 31 項），**已達目標狀態，無需搬移**。

### 2026-09-09（第 1 次，單向版）
| 資料夾 | 檔案數 | 動作 | 原因 |
| :--- | :---: | :--- | :--- |
| `00386中石化` | 7 | 移至 `History/` | 不在輪值表 |
| `00857中石油` | 7 | 移至 `History/` | 不在輪值表 |
| `6361荏原製作所` | 7 | 移至 `History/` | 不在輪值表 |
| `INGR宜瑞安` | 2 | 移至 `History/` | 不在輪值表 |

- 反向檢查、同名衝突：皆無。
